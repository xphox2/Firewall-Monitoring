{
  "name": "firewall-mon",
  "private": true,
  "description": "Firewall monitoring system — Tailwind CSS build tooling only (the app version lives in cmd/api/main.go ServerVersion). AUDIT-134: no version field here; it was unused and went stale.",
  "scripts": {
    "tailwind": "tailwindcss -i ./cmd/api/static/css/styles.css -o ./cmd/api/static/css/tailwind.css --minify",
    "tailwind:watch": "tailwindcss -i ./cmd/api/static/css/styles.css -o ./cmd/api/static/css/tailwind.css --watch",
    "build": "npm run tailwind",
    "prebuild": "npm run tailwind"
  },
  "dependencies": {
    "tailwindcss": "^3.4.19"
  }
}
