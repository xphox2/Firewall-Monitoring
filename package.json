{
  "name": "firewall-mon",
  "private": true,
  "description": "Firewall monitoring system — Tailwind CSS build tooling only (the app version lives in cmd/api/main.go ServerVersion). AUDIT-134: no version field here; it was unused and went stale.",
  "scripts": {
    "tailwind": "tailwindcss -i ./cmd/api/static/css/styles.css -o ./cmd/api/static/css/tailwind.css --minify",
    "tailwind:watch": "tailwindcss -i ./cmd/api/static/css/styles.css -o ./cmd/api/static/css/tailwind.css --watch",
    "build": "npm run tailwind",
    "prebuild": "npm run tailwind"
  },
  "dependencies": {
    "tailwindcss": "^4.3.2"
  },
  "_comment_vendoredBrowserLibraries": "AUDIT-160: provenance/version pins for the pre-minified browser libs committed under cmd/api/static (embedded via go:embed, NOT npm-installed). Full table incl. upstream URLs + licenses in cmd/api/static/js/VENDOR.md. The three cytoscape-fcose-stack files have no version embedded in their artifact; pin on next refresh.",
  "vendoredBrowserLibraries": {
    "chart.js": "4.4.7",
    "chartjs-plugin-zoom": "2.0.1",
    "cytoscape": "3.30.4",
    "gridstack": "10.3.1",
    "uplot": "1.6.31"
  }
}
