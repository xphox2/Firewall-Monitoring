# 2026-07-04 Logic & Consistency Audit

Dual-repo audit (Firewall-Mon server + Firewall-Collector) targeting logical flaws and
inconsistencies accumulated over the last three months of feature work (server
v0.10.x -> v0.11.21, collector -> v1.3.0), with emphasis on the Tranche 1 access-control,
Tranche 2 alerting, MFA/profile, and Tranche 3 NetFlow/IPFIX waves and their interaction
with pre-existing code.

**Method:** 13-dimension multi-agent sweep (cross-repo contract, NetFlow/IPFIX pipeline,
alerting lifecycle, RBAC coverage, auth state machines, migrations, retention, time/units
math, concurrency, frontend/backend agreement, config coherence, sibling divergence,
stale artifacts) + completeness-critic gap round. Every finding adversarially verified by
2-3 independent refutation lenses (correctness / design-intent / reachability-impact);
high-severity findings required 2-of-3 confirmation.

**Stats:** 64 raw findings -> 51 after dedup -> 50 confirmed + 3 gap-round findings = 53
confirmed (51 unique after cross-dimension merges), 1 refuted.

Finding IDs are LC-nn. Severity counts (unique): HIGH 5 / MEDIUM 26 / LOW 20.

---

### LC-20 [HIGH] (server) — Five per-poll status tables (vpn_status, ha_status, security_stats, sdwan_health, license_info) have NO retention path — unbounded growth, missed by both prior retention passes

**Status:** OPEN

**Location:** `internal/database/cleanup.go:228` · Dimension: db-migrations,retention-cleanup

CleanupOldData's entries list is the only generic retention mechanism, and it covers system_status, interface_stats, processor_stats, hardware_sensors, trap_events, login_attempts, flow_samples, flow_if_counters, interface_addresses, ping_results, interface_errors, process_stats, irc_message_logs, processed_batches, flow_rollups (cleanup.go:228-263) plus flow_detections/flow_agent_drops/syslog/alerts special cases. It never touches vpn_status, ha_status, security_stats, sdwan_health, or license_info — yet all five are APPENDED per poll cycle by the poller (cmd/poller/main.go:823 SaveVPNStatuses inside pollDevice) and per push by every remote collector (handlers_data.go:725/772/810/848/881). vpn_status alone gets one row per tunnel per poll (charted historically by GetVPNChartWindow, charts.go:420), i.e. tunnels x 1440 rows/day at a 60s interval, forever. AUDIT-029 fixed 'the four tables that previously had no retention knob' and 2026-07-01 H4 fixed the three flow tables, but both passes missed this sibling family; no prior audit report or docs/AUDIT.md entry mentions vpn_status retention (only AUDIT-147 autovacuum). docs/DATA-RETENTION.md:51-59 ('Tables that are NOT auto-pruned') claims only config_revisions and audit_logs are not auto-pruned, which is false for these five tables.

**Evidence:** cleanup.go:228-263 entries := []cleanupEntry{ {&models.SystemStatus{}, "system_status", statusDays}, ... {&models.FlowRollup{}, "flow_rollups", ...} } — no VPNStatus/HAStatus/SecurityStats/SDWANHealth/LicenseInfo entry. Writers: internal/database/telemetry.go:128-130 `func (d *Database) SaveVPNStatuses(statuses []models.VPNStatus) error { return batchInsertWithFallback(d.db, "vpn_status", statuses) }` (append, not upsert); internal/database/ping.go:132-143 SaveHAStatuses/SaveSecurityStats/SaveSDWANHealth all batchInsertWithFallback; ping.go:199-204 SaveLicenseInfo `d.db.Create(&licenses)`. Call sites: cmd/poller/main.go:823 `p.db.SaveVPNStatuses(vpnStatuses)` per poll; internal/api/handlers/handlers_data.go:725/772/810/848/881 per probe batch. No Delete path exists anywhere (grep for Delete(&models.VPNStatus etc. only hits reads). docs/DATA-RETENTION.md:51-59: 'Tables that are NOT auto-pruned: config_revisions ... audit_logs' — contradicted by these five tables.

**Suggested fix:** Add cleanupEntry rows for the five tables (statusDays fits vpn_status/ha_status/security_stats/sdwan_health; license_info can use a longer knob), add RETENTION_* knobs if per-table tuning is wanted, and update DATA-RETENTION.md's table + not-pruned section.

### LC-25 [HIGH] (server) — Flow analytics 7d/30d/90d windows query a single rollup interval_type while the rollup lifecycle keeps each age band in exactly one tier — 7d/30d views silently omit the (1h,48h] band and 90d omits (1h,30d]

**Status:** OPEN

**Location:** `internal/database/flows.go:227` · Dimension: time-units-math

GetFlowStats picks ONE rollup interval per query window: `rollupInterval := "5m"; if hours > 48 { rollupInterval = "1h" }; if hours > 720 { rollupInterval = "1d" }` and then filters `interval_type = ?` (flows.go:235). But RunFlowRollupCycle (flows.go:673-692) keeps each age band in exactly ONE tier and DELETES the source rows on promotion: raw flows exist only for the last ~1h (consumed into 5m rollups, raw deleted flows.go:776-778), 5m rollups exist only for (1h,48h] (promoted to 1h at 48h and deleted, flows.go:860-863), 1h rollups exist only for (48h,30d], 1d rollups only for >30d. So a 7d query (hours=168 → interval '1h') gets raw last-hour + 1h rollups (48h..7d) and returns ZERO data for the (1h,48h] band — ~28% of the window, and the most recent, most interesting part. A 30d query misses the same band; a 90d query (hours=2160 → '1d') misses (1h,30d] — a third of its window. Every downstream figure is wrong: TotalBytes/TotalFlows, BitsPerSecond (flows.go:300 divides the under-counted bytes by the FULL window), top talkers, protocol mix, and the bytes-over-time chart shows a near-flat recent region. The UI exposes exactly these ranges (admin.html range pills data-range 168/720/2160). The same copy-pasted selection exists in GetConnectionFlowStats (internal/database/connection_detail.go:1017-1027). The comment '(best available interval for the time range)' (flows.go:226) shows the author assumed each interval tier spans the whole range — it never does, because promotion deletes the source tier.

**Evidence:** flows.go:227-235: `rollupInterval := "5m"\n\tif hours > 48 {\n\t\trollupInterval = "1h"\n\t}\n\tif hours > 720 { // 30 days\n\t\trollupInterval = "1d"\n\t}\n\tnewRollupBase := ... Where("timestamp > ? AND interval_type = ?", cutoff, rollupInterval)` — vs flows.go:676-690: `// Step 1: raw flows > 1h old → 5m rollups ... // Step 2: 5m rollups > 48h old → 1h rollups ... // Step 3: 1h rollups > 30d old → 1d rollups` and flows.go:860-863: `tx.Where("interval_type = ? AND timestamp < ? AND id <= ?", srcInterval, cutoff, watermark).Delete(&models.FlowRollup{})` (source tier destroyed on promotion, so tiers are disjoint). Sibling: connection_detail.go:1017-1027 identical pattern. UI ranges: web/admin/admin.html:474-475,546 `data-range="168" ... data-range="720" ... data-range="2160"`.

**Suggested fix:** Query rollups with `interval_type IN (...)` covering every tier inside the window (raw + 5m + 1h + 1d as applicable) — the tiers are disjoint by construction so summing them cannot double-count; or per-band queries (5m for (1h,48h], 1h for (48h,30d], 1d beyond). Apply the same fix to GetConnectionFlowStats.

### LC-37 [HIGH] (server) — Settings save writes the '********' redaction mask over pagerduty_routing_key / opsgenie_api_key / webhook_secret — mask-skip exists only for smtp_password

**Status:** OPEN

**Location:** `internal/api/handlers/handlers_settings.go:261` · Dimension: config-settings

GetSettings masks all four settingsSecretKeys as '********' (handlers_settings.go:59-63). The admin UI renders webhook_secret, pagerduty_routing_key, opsgenie_api_key as password inputs pre-filled with that masked value, and saveSettings() collects '#settings-notifications input' with NO mask skip (admin-main.js:2898-2900) — the sibling '#settings-smtp input' collector DOES skip masked passwords (admin-main.js:2902-2903). Server-side, UpdateSettings' switch only skips the mask inside `case "smtp_password"` (handlers_settings.go:261-265); the three T2-5 secret keys match no case and fall through to persistence at lines 315-331 ('********' != "" so it is saved). Result: the first save of ANYTHING on the Settings page after a page load destroys the stored PagerDuty routing key, Opsgenie API key, and webhook signing secret. AlertManager.RefreshThresholds then loads '********' as the live credential (internal/alerts/alerts.go:720-731) and the notifier posts routing_key '********' to PagerDuty (internal/notifier/incident_channels.go:73). GetSettings re-masks on every read, so the corruption is invisible in the UI. This is exactly the documented '********'-writeback bug class (internal/httputil/redact.go:5-11) recurring in the Tranche 2 settings added v0.11.1-.10; the existing regression test handlers_redacted_writeback_test.go only covers devices.

**Evidence:** handlers_settings.go:261-265: `case "smtp_password":\n  // Skip masked passwords\n  if s.Value == "********" {\n    continue\n  }` — the ONLY mask skip in UpdateSettings. handlers_settings.go:30-35 marks four keys secret: `var settingsSecretKeys = map[string]bool{"smtp_password": true, "webhook_secret": true, "pagerduty_routing_key": true, "opsgenie_api_key": true}`; GetSettings:59-63 masks them: `if settingsSecretKeys[settings[i].Key] { settings[i].Value = "********" }`. cmd/api/static/js/admin-main.js:2898-2900 (notifications section containing the three T2 password inputs, defined at 2403-2405): `document.querySelectorAll('#settings-notifications input').forEach(function(input) { settings.push({ key: input.name, value: ... input.value, ...}); });` — no mask check — vs the SMTP sibling at 2902-2903: `document.querySelectorAll('#settings-smtp input').forEach(function(input) { if (input.type === 'password' && input.value === '********') return; ...})`. Persistence at handlers_settings.go:315-316: `if !s.IsSecret || s.Value != "" { existing.Value = s.Value` — '********' passes. Consumption: alerts.go:720-722 `case "pagerduty_routing_key": ... am.config.Alerts.PagerDutyRoutingKey = am.db.DecryptField(s.Value)` (DecryptField is idempotent on non-{enc} values, so it returns '********').

**Suggested fix:** Hoist the mask skip out of the smtp_password case: before the switch, `if settingsSecretKeys[s.Key] && s.Value == httputil.RedactedMask { continue }`. Mirror the '********' skip into the #settings-notifications collector in admin-main.js. Add a settings variant of the redacted-writeback regression test covering all settingsSecretKeys.

### LC-42 [HIGH] (server) — PagerDuty/Opsgenie incidents opened by alerts are never auto-resolved: recovery path never reaches incident channels AND the dedup key can't match anyway

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:799` · Dimension: sibling-divergence

T2-5 (v0.11.7) designed PagerDuty/Opsgenie as STATEFUL: incident_channels.go:13-15 says 'the companion *_RESOLVED events resolve/close the incident'. Two independent bugs make the resolve half unreachable in production. (1) sendRecovery builds its NotifyConfig with notifier.SnapshotConfig (alerts.go:799), which never sets PolicyActive/Enable* (notifier.go:102-118), while incidentChannelEligibility hard-gates on `nc.PolicyActive && nc.EnablePagerDuty` (notifier.go:139-141) — so no *_RESOLVED alert can ever be sent to PagerDuty/Opsgenie/Teams. Fires DO go out (dispatchFired uses BuildNotifyConfigFromResolved with PolicyActive=true, policy.go:372). (2) Even if a resolve got through, alertDedupKey includes a.MetricName (incident_channels.go:23-26), and the fire carries the real metric ('cpu_usage', 'device_status') while sendRecovery's companion record hardcodes `MetricName: "recovery"` (alerts.go:843) and closeIncident's summary hardcodes `MetricName: "incident"` + AlertType INCIDENT_RESOLVED (incidents_f12.go:99-103) — neither can reproduce the trigger's dedup_key/alias. Net effect: every PD incident/Opsgenie alert opened by firewall-mon stays open until a human closes it; sendOpsgenie's close-by-alias posts to an alias that never existed. The unit test masks this by fabricating a recovery alert shape production never produces (incident_channels_test.go:23-27 sets a.MetricName = "cpu_usage").

**Evidence:** internal/notifier/incident_channels.go:23-26: `func alertDedupKey(a *models.Alert) string { t := strings.TrimSuffix(string(a.AlertType), "_RESOLVED"); return fmt.Sprintf("fwmon-%d-%s-%s", a.DeviceID, t, a.MetricName) }`. internal/alerts/alerts.go:838-843 (recovery companion): `AlertType: alertType + "_RESOLVED", ... MetricName: "recovery"` sent at :852 with nc from :799 `nc := notifier.SnapshotConfig(&am.config.Alerts)`. internal/notifier/notifier.go:139-141: `pagerduty = nc.PagerDutyRoutingKey != "" && nc.PolicyActive && nc.EnablePagerDuty` (PolicyActive absent from SnapshotConfig, notifier.go:101-118). Test fabricates matching MetricName: internal/notifier/incident_channels_test.go:23-27 `a.AlertType = "CPU_HIGH_RESOLVED"; a.MetricName = "cpu_usage"`.

**Suggested fix:** In sendRecovery, resolve the device/policy config (resolveAlertConfig + BuildNotifyConfigFromResolved) like the fire path does, and either preserve the fire's MetricName on the companion record or base alertDedupKey on fields both records share (device_id + trimmed alert_type).

### LC-50 [HIGH] (server) — Flow detection engine treats Tranche 3 DENIED firewall-event rows as forwarded traffic — blocked internet background radiation produces perpetual false 'cleartext traffic' / 'egress' / 'known-bad traffic' warnings

**Status:** OPEN

**Location:** `internal/detect/security.go:45` · Dimension: gap-sweep

No audit dimension owned internal/detect (the R3/R4 sFlow detection engine the poller runs every 5 minutes). Tranche 3 deliberately stores zero-byte DENIED records (firewall_event=3) in the same flow_samples table the detectors aggregate, but not one of the 9 registered detectors filters or even reads firewall_event — grep shows models.FirewallEventDenied (models.go:993) is referenced NOWHERE outside its definition. Consequences: (1) cleartextDetector has no direction filter and fires at COUNT(*)>=3 ('minFlows=3'), so three DENIED inbound Telnet/FTP probes in 15 minutes — universal internet background radiation that the firewall successfully blocked — produce a 'Cleartext Telnet traffic' warning every cycle on every NSEL/PAN-OS/FortiGate NetFlow exporter; (2) unexpectedEgressDetector asserts 'SMB (port 445) leaving the network outbound' for outbound attempts the firewall policy denied — factually false, nothing left the network; (3) threatIntelDetector reports 'Traffic with known-bad destination' for connections that were blocked, indistinguishable from real compromise traffic. Each detection is persisted and fed to the alert engine (cmd/poller/main.go:406-427 runFlowDetectionCycle → ProcessFlowDetection). The design doc's Tranche 4 backlog defers ADDING denied-flow detectors ('deny storms, denied-then-allowed', item 3) and sampled-vs-complete threshold gating (item 1), but nothing defers making existing detectors stop asserting that blocked traffic occurred — that is a shipped-now correctness bug in the interaction between the pre-T3 engine and T3 ingestion.

**Evidence:** Detectors query flow_samples with no firewall_event predicate — port scan: `w.DB.Model(&models.FlowSample{}).Where("timestamp >= ? AND timestamp < ?", w.Start, w.End).Select("src_addr, COUNT(DISTINCT dst_port) as ports...").Group("src_addr")` (internal/detect/security.go:45-50); cleartext (no direction filter): `Where("dst_port IN ?", cleartextPorts)...Having("COUNT(*) >= ?", minFlows)` with `minFlows = 3` (internal/detect/detectors.go:52-58, 13); unexpected_egress message: `"%s (port %d) leaving the network outbound: %d flows"` (detectors.go:105); threat_intel message: `"Traffic with known-bad %s: %s → %s"` (security.go:194). Ingest deliberately legalizes the rows: `// NOTE: zero-byte rows are deliberately legal — NSEL create/denied records ... denied-flow visibility (firewall_event=3) is the headline NetFlow feature.` (internal/api/handlers/handlers_data.go:252-254). `grep -rn FirewallEventDenied --include='*.go'` matches only its declaration at internal/models/models.go:993 — zero consumers anywhere, including all of internal/detect.

**Suggested fix:** Exclude event-only rows from traffic-assertion detectors (WHERE firewall_event NOT IN (3) at minimum, arguably also 1/4/5 which carry no/partial counters), or add an explicit firewall_event validity class per detector; port_scan/super_spreader may legitimately keep denied rows but must say 'blocked' in the message and severity.

### LC-00 [MEDIUM] (collector) — Dual-export flow dedup keys sFlow by in-band agent address but NetFlow by UDP source IP — suppression silently no-ops when they differ, defeating the Tranche 3 double-count defense

**Status:** RESOLVED (collector v1.3.2)

**Location:** `cmd/collector/main.go:505` · Dimension: cross-repo-contract

The v1.3.0 flowdedup tracker is supposed to be a per-device source preference (design record: server docs/flow-protocol-research-2026-07-03.md line 113 'Per-device source preference'; FortiGate is called out as 'primary dedup vendor'). The implementation keys both families by SamplerAddress strings with DIFFERENT semantics: the sFlow parser sets SamplerAddress from the datagram's in-band agent address field (sflow.go parses agent IP out of the payload), while the NetFlow/IPFIX parsers set it from the UDP source address (remoteAddr.IP). On FortiGate — the one vendor the feature targets — the sFlow agent-address and the NetFlow export source IP are independently configurable and commonly differ (agent-address defaults to a management/loopback identity; NetFlow leaves from the egress interface). When they differ, flowdedup.Tracker sees two unrelated 'exporters', SuppressSFlowFlow/SuppressNetFlow never observe cross-family activity, and both families are relayed — every byte double-counted in device-agnostic server aggregates. The failure is silent on the server too: ReceiveFlowSamples resolves device_id by exact ip_address match, so at most one of the two addresses maps to the device row, and GetMixedFlowSourceDevices (the advisory banner) filters `device_id > 0` HAVING COUNT(DISTINCT flow_source) > 1 — with one family stuck at device_id=0 the mixed-source warning never fires either.

**Evidence:** Collector main.go:505 `if c.flowDedup.SuppressSFlowFlow(sample.SamplerAddress, time.Now())` vs main.go:549 `if c.flowDedup.SuppressNetFlow(sample.SamplerAddress, time.Now())`. sFlow side: internal/sflow/sflow.go:318-330 parses `agentIP` from the datagram payload (`agentIP = net.IP(data[offset:offset+4]).To16()`), sflow.go:357 `agentAddr := agentIP.String()`, sflow.go:471 `SamplerAddress: agentAddr`. NetFlow side: internal/netflow/netflow.go:376 `r.handleDatagram(buf[:n], remoteAddr.IP)`; v5.go:47-48 'exporterIP is the UDP source address (always the SamplerAddress — v5 has no in-band agent address)'; record.go:464 `SamplerAddress: ctx.key.exporter`. Server banner blind spot: internal/database/flows.go:80-83 `SELECT device_id FROM flow_samples WHERE timestamp > ? AND device_id > 0 GROUP BY device_id HAVING COUNT(DISTINCT flow_source) > 1`; ingest device resolution is exact-IP only (handlers_data.go:213-231 ResolveDevicesByIPs on SamplerAddress).

**Suggested fix:** Key the dedup tracker by resolved device (resolveDeviceByIP result, falling back to address only when unresolved), or track both the in-band agent address and the UDP source per exporter. Server-side, extend the mixed-source query to also group unattributed rows by sampler_address.

### LC-01 [MEDIUM] (both) — Decommissioned probe returns 410 on heartbeat/register but 403 on all 20 data-plane endpoints — collector taxonomy reads 403 as 're-register', producing a permanent re-register/requeue loop instead of the documented non-retryable quiesce

**Status:** OPEN

**Location:** `internal/api/handlers/handlers_probes.go:820` · Dimension: cross-repo-contract

The M7 fix (2026-07-01 audit) gated all three planes against decommissioned/disabled probes, but the three server handlers diverged on status code for the SAME lifecycle state: RegisterProbe and ProbeHeartbeat return 410 Gone (explicitly documented as 'non-retryable — the probe is gone on purpose, not that its auth is transiently broken'), while validateProbe — the gate in front of every ingestion endpoint (/syslog, /flows, /pings, all 20) — returns 403 Forbidden. The collector's status taxonomy assigns those two codes opposite semantics: 410 is a permanent 4xx (isRetryableStatus → drop batch), but 403 triggers `c.approved.Store(false)` + tryReregister and, on re-registration failure, returns (delivered=false, permanent=false) → requeue as TRANSIENT. Net behavior after an admin decommissions a probe: every batch send gets 403 → collector deapproves, calls Register → Register gets 410 → error → tryReregister false → batch requeued; repeat every sync cycle forever, with re-registration attempts every 60s plus 10-minute cooldown cycles indefinitely, and all queues spooling to the disk byte cap. The server's own 410 convention never reaches the data plane where the collector's permanent/transient decision is actually made, so the M7 'non-retryable' contract is unimplementable from the collector's side.

**Evidence:** Server: handlers_probes.go:547 `probeErr(c, http.StatusGone, "Probe is decommissioned or disabled — re-commission it in the admin UI first")` (RegisterProbe) and :686 `probeErr(c, http.StatusGone, "Probe is rejected or decommissioned — heartbeat refused")` with comment '410 tells the collector the probe is gone on purpose (non-retryable)' vs :820 `c.JSON(http.StatusForbidden, response.Error("Probe is decommissioned or disabled"))` (validateProbe, all ingestion routes). Collector: internal/relay/relay.go:1701 classifies 410 non-retryable/permanent (`case 400, 401, 403, 404, 405, 409, 410, 422: return false`), relay.go:1760-1768 `if resp.StatusCode == 404 || resp.StatusCode == 401 || resp.StatusCode == 403 { ... c.approved.Store(false); if c.tryReregister() { continue }; return false, false }` (transient → requeue); tryReregister relay.go:924-957 resets its counter every 10 minutes forever.

**Suggested fix:** Make validateProbe return 410 for DecommissionedAt/!Enabled like its two siblings (keep 403 for not-approved), and teach the collector an explicit 410 branch: stop re-registering, keep spooled data, log once, and back off to a slow recommission probe.

### LC-02 [MEDIUM] (both) — Collector heartbeat treats every non-401/403 response as success — server's 410 Gone, 429, 400 and 5xx are silently counted as healthy heartbeats, so /readyz and Prometheus report success while the server refuses to update last_seen and marks the probe offline

**Status:** OPEN

**Location:** `internal/relay/relay.go:1049` · Dimension: cross-repo-contract

sendHeartbeatWithStatus checks only `resp.StatusCode == 401 || resp.StatusCode == 403`; every other status — including the server's deliberate 410 Gone for a rejected/decommissioned probe (which returns BEFORE writing last_seen/status), a 429 from the /api group rate limiter, a 400 'Invalid status value', or any 5xx — falls through to `return nil`. runHeartbeatLoop then records `*last = time.Now()` (read by /readyz) and calls metrics.OnHeartbeatSuccess(). Consequences: (a) the server stops refreshing last_seen, MarkStaleProbesOffline flips the probe to offline in the admin UI, yet the collector's readiness endpoint and heartbeat-success metric report a fully healthy relay — the exact monitoring signals an operator would check first point the wrong way; (b) the 410 signal the server added in M7 ('heartbeat refused') has no consumer at all — the collector keeps POSTing heartbeats at full cadence forever with zero log output, because the refusal is swallowed as success.

**Evidence:** Collector: internal/relay/relay.go:1032 `if resp.StatusCode == 401 || resp.StatusCode == 403 {` is the ONLY status branch in sendHeartbeatWithStatus; relay.go:1049 `return nil` for everything else (no logging, body not read). cmd/collector/main.go:611-618 `if err := c.relayClient.SendHeartbeat(); err != nil { ... OnHeartbeatFailure() } else { *last = time.Now(); ... OnHeartbeatSuccess() }`. Server: handlers_probes.go:685-687 `if probe.ApprovalStatus == "rejected" || probe.DecommissionedAt != nil || !probe.Enabled { probeErr(c, http.StatusGone, ...) }` returns before the last_seen/status update at :702-705, and :692-694 returns 400 for an invalid status value — both swallowed as success by the collector.

**Suggested fix:** In sendHeartbeatWithStatus, return an error for any non-2xx (with the server's error body), add a distinct 410 branch that stops the re-register churn, and only then let runHeartbeatLoop count success/refresh the /readyz timestamp.

### LC-03 [MEDIUM] (server) — Tranche 3 forensic columns (firewall_event/denied flows, post-NAT, app_name, flow_start/end) are destroyed after ~1 hour by the rollup cycle

**Status:** OPEN

**Location:** `internal/database/flows.go:677` · Dimension: netflow-ipfix

Migration v29 added flow_source to both flow_samples and flow_rollups, but NONE of the other new NetFlow columns survive the rollup ladder. RunFlowRollupCycle aggregates raw flow_samples older than 1 hour into 5m rollups and DELETES the raw rows in the same transaction. FlowRollup (models.go:1075-1105) carries only bytes/packets/flow_count plus the group key (…, dst_asn, flow_source) — no firewall_event, flow_end_reason, post_nat_*, app_name, flow_start/flow_end, icmp_type_code. A zero-byte NSEL/FortiGate denied record (firewall_event=3, the feature the v0.11.20 commit and migrate.go v29 comment both call 'the headline NetFlow win') is therefore rolled into an anonymous flow_count increment and erased after 60 minutes — the ingest handler carefully preserves zero-byte denied rows (handlers_data.go:252-254) only for the rollup cycle to delete them an hour later. The design record's scaling section (docs/flow-protocol-research-2026-07-03.md line 198) plans a '30d raw retention env var' and pitches an 'honest full-fidelity raw window', but the shipped code hard-deletes raw flows at 1h with no env override. Post-NAT correlation and Tranche 4 flow stitching (flow_end_reason=2) are equally limited to a 1-hour window.

**Evidence:** flows.go:677-678 `cutoff1h := time.Now().Add(-1 * time.Hour); if d.aggregateFlowsToRollup(cutoff1h, "5m")`; flows.go:776-777 `tx.Where("timestamp < ? AND id <= ?", cutoff, watermark).Delete(&models.FlowSample{})`; flows.go:709 group key = `"bucket, device_id, src_addr, dst_addr, dst_port, protocol, app_category, direction, dst_country, dst_asn, flow_source"` (no firewall_event); models.go:1075-1105 FlowRollup struct has no firewall_event/post_nat/app_name/flow_start fields; migrate.go:1322-1323 justifies the columns because 'denied-flow visibility is the headline NetFlow win'; grep confirms firewall_event is never read by any query, detector, or UI file (only written in flow_samples_pgx.go:51,112).

**Suggested fix:** Either add firewall_event (at minimum the denied=3 distinction, e.g. a denied_count column) to the rollup group/schema, or exempt firewall_event!=0 rows from the rollup delete, or ship the planned RETENTION_FLOW_RAW env so operators can keep raw NetFlow rows longer than 1h.

### LC-05 [MEDIUM] (server) — models.go documents AppName as 'outranking the port heuristic in app_category' but ingest never consults it — PAN App-ID is stored dead

**Status:** OPEN

**Location:** `internal/models/models.go:964` · Dimension: netflow-ipfix

The FlowSample model comment states the exporter-provided application name is 'vendor truth that outranks the port heuristic in app_category when present', and the collector goes to real lengths to extract it (record.go:161-164 PEN-25461 PAN App-ID, record.go:259-260 v9-squatted fieldPANAppID, length-capped at 64 for 'the server column'). But ReceiveFlowSamples computes AppCategory purely from `classify.Classify(Protocol, SrcPort, DstPort, TCPFlags)` (handlers_data.go:258) and never reads samples[i].AppName; internal/classify has no app-name-aware entry point; no query, detector, or admin-flows.js code reads app_name back out (repo-wide grep matches only the syslog table's unrelated app_name at handlers_analytics.go:214 and the COPY column list). A PAN flow identified by the firewall as e.g. 'ssl' on a non-standard port is classified Unknown, contradicting the documented contract, and the varchar(64) column added by migration v29 plus the collector's whole PAN App-ID decode path is write-only.

**Evidence:** models.go:964-966 `// AppName is the EXPORTER's application identification (PAN App-ID 56701, // FortiGate app options) — vendor truth that outranks the port heuristic // in app_category when present.` vs handlers_data.go:258 `samples[i].AppCategory = uint8(classify.Classify(samples[i].Protocol, samples[i].SrcPort, samples[i].DstPort, samples[i].TCPFlags))` (AppName unused); grep of internal/api/handlers, internal/classify, internal/database/flows.go, cmd/api/static/js returns zero readers of FlowSample.AppName/app_name; collector record.go:161-164 + 259-260 populate it.

**Suggested fix:** Either implement the documented precedence (map known exporter app names to classify.Category when AppName != "" before the port heuristic) and surface app_name on the Flows samples view, or correct the model comment to say the field is stored for future use.

### LC-06 [MEDIUM] (collector) — Sampling-rate override (documented precedence step 1, the MikroTik ROS6 escape hatch) is unreachable dead code — no config knob, no caller

**Status:** OPEN

**Location:** `internal/netflow/netflow.go:168` · Dimension: netflow-ipfix

The sampler-resolution chain is documented as '1. operator override for the exporter IP (SetSamplingOverride — the escape hatch for MikroTik ROS6's little-endian rate bug…)' (template.go:210-215), and the persistence section asserts 'operator overrides do NOT persist — they are configuration, re-applied at startup by the caller' (template.go:300-302). But nothing ever calls NetFlowReceiver.SetSamplingOverride: cmd/collector/main.go never invokes it, internal/config/config.go has no PROBE_*SAMPLING* variable, and ENV-VARS.md has no entry. Step 1 of the documented four-step precedence is unreachable; a MikroTik ROS6 exporter with the byte-swapped rate bug (which the design explicitly says both goflow2 and Akvorado refuse to auto-guess) has no operator remedy — its flows are silently mis-scaled by the bogus learned rate with no way to pin the correct one.

**Evidence:** netflow.go:168-173 `// SetSamplingOverride pins the sampling rate for an exporter IP… func (r *NetFlowReceiver) SetSamplingOverride(exporterIP string, rate uint32)`; template.go:301-302 `operator overrides do NOT persist — they are configuration, re-applied at startup by the caller.`; `grep -rn SetSamplingOverride` over the collector repo matches only internal/netflow/{netflow.go:171-172, template.go:210,235,238} and tests — zero call sites in cmd/ or config; config.go:164-173 defines only NetFlowEnabled/Port/IPFIXPort/RateLimit/FlowDedupPolicy.

**Suggested fix:** Add a PROBE_NETFLOW_SAMPLING_OVERRIDES env (e.g. "ip=rate,ip=rate") parsed in config.go and applied in main.go after netflowReceiver construction (and re-applied on settings refresh), or delete the API and the step-1 documentation.

### LC-09 [MEDIUM] (server) — Maintenance windows and alert-policy edits never take effect in the API and trap-receiver alert paths (policy cache frozen at startup)

**Status:** RESOLVED (server v0.11.23)

**Location:** `cmd/api/main.go:361` · Dimension: alerting,concurrency-state,config-settings

Three processes embed an AlertManager, but only the poller refreshes its policy/maintenance/threshold cache. The API process (which fires SYSLOG_* alerts from probe batches at handlers_data.go:148, SSH_HOST_KEY_CHANGED at handlers_probes.go:748, CONFIG_CHANGE at handlers_data.go:1098) and the trap-receiver (ProcessTrap at cmd/trap-receiver/main.go:141) call RefreshThresholds exactly once at startup and never again — there is no ticker and no settings-save hook (grep of internal/api/handlers shows no RefreshThresholds/RefreshPolicyCache call). Worse, the cache loader GetActiveMaintenanceWindows only returns windows active AT LOAD TIME (start_time <= now AND end_time >= now), so even a maintenance window scheduled in advance, before the process started, never enters the API/trap-receiver cache. Net effect: maintenance windows silence poller-driven alerts (CPU, interface, VPN, offline) but syslog-critical, trap, SSH-hostkey and config-change alerts fire and notify right through every maintenance window; alert-policy edits (enable/disable rule, severity, cooldown, channel routing) and SMTP/webhook credential changes made in the admin UI also never reach those two processes until restart, while the poller picks them up within one cycle — contradictory behavior across alert types that looks like random policy non-enforcement to operators.

**Evidence:** cmd/api/main.go:361-363: `alertMgr := alerts.NewAlertManager(cfg, notif, db)` / `alertMgr.RefreshThresholds(db.Gorm())` / `handler.SetAlertManager(alertMgr)` — the only RefreshThresholds call in the API process (tickers at main.go:303/324/341 refresh login attempts, threat intel, GeoIP — not the AlertManager). cmd/trap-receiver/main.go:134-135: `alertManager := alerts.NewAlertManager(cfg, notif, db)` / `alertManager.RefreshThresholds(db.Gorm())` — once, before Start(). Contrast cmd/poller/main.go:493-495: `// Refresh alert thresholds from DB so admin UI changes take effect` / `p.alertManager.RefreshThresholds(p.db.Gorm())` inside pollAllDevices (every cycle). internal/database/alerts.go:164-168: `GetActiveMaintenanceWindows` uses `Where("start_time <= ? AND end_time >= ?", now, now)` so future-scheduled windows are excluded from the startup snapshot. internal/alerts/policy.go:262-290 consults only the cached `am.policyCache.windows` for InMaintenance.

**Suggested fix:** Run a periodic RefreshThresholds ticker (e.g. 1 min) in cmd/api and cmd/trap-receiver, and/or change GetActiveMaintenanceWindows to also return future windows (end_time >= now) since resolveAlertConfig already re-checks start/end against time.Now().

### LC-10 [MEDIUM] (server) — CheckEscalations ignores snooze: a snoozed alert keeps escalating/notifying through the snooze window

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:1334` · Dimension: alerting,time-units-math

The snooze feature is documented as 'temporarily silences an alert' and is explicitly 'Distinct from acknowledge' (the operator deliberately leaves it unacked so it resurfaces). But the escalation engine's query GetUnacknowledgedAlerts filters only on acknowledged=false AND suppressed=false — it never checks snoozed_until. So the canonical operator pattern ('snooze for 4 hours while I finish unrelated triage') does not stop escalation: the poller keeps re-sending [ESCALATION n/N] notifications (legacy cadence) or firing F19 step chains (PagerDuty/Opsgenie tiers included) for the exact alert the operator just silenced, every EscalationMinutes, for up to 24h. The UI hides the alert (applyAlertFilters excludes snoozed rows), making the continued paging look inexplicable. The T2 escalation additions (migrations v26/F19) were built on GetUnacknowledgedAlerts without updating it for the earlier snooze feature.

**Evidence:** internal/database/alerts.go:344-349: `func (d *Database) GetUnacknowledgedAlerts(since time.Time) ... Where("acknowledged = ? AND suppressed = ? AND timestamp > ?", false, false, since)` — no snoozed_until predicate. internal/database/alerts.go:291-303: SnoozeAlertsBulk updates only `snoozed_until/snoozed_by/snoozed_reason`, never acknowledged. internal/api/handlers/handlers_analytics.go:693-696: `// SnoozeAlert temporarily silences an alert until SnoozedUntil. Distinct from acknowledge: the alert resurfaces in the default list once the snooze expires`. internal/alerts/alerts.go:1334 `alerts, err := am.db.GetUnacknowledgedAlerts(cutoff)` feeding both the F19 step loop (1375-1394) and legacy re-send (1396-1416) with no snooze check. handlers_analytics.go:99-101 hides snoozed rows from the UI: `q = q.Where("snoozed_until IS NULL OR snoozed_until < ?", time.Now())`.

**Suggested fix:** Add `AND (snoozed_until IS NULL OR snoozed_until < now)` to GetUnacknowledgedAlerts (or skip alerts with an active snooze inside CheckEscalations).

### LC-11 [MEDIUM] (server) — F12 incident muting is defeated by escalation: incident-attached alerts whose initial notification was muted escalate individually later

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:893` · Dimension: alerting,sibling-divergence

F12 incident grouping mutes the individual notification of every alert that fires while a device has an open incident ('the incident is the story', kills the 'switch reboot = 8 emails' storm). But muting is implemented as a local `incidentMuted` bool in dispatchFired — the saved row keeps Suppressed=false and acknowledged=false. CheckEscalations then picks these rows up via GetUnacknowledgedAlerts (which filters only acknowledged/suppressed) and re-sends each one individually with '[ESCALATION 1/N]' / step-chain notifications once EscalationMinutes elapse. So on any policy with escalation enabled, the storm F12 was built to kill is resurrected wholesale a few minutes later — and each muted alert's FIRST notification arrives as an escalation, which is more alarming than the original would have been. Flap- and maintenance-suppressed alerts are correctly excluded (they save Suppressed=true); incident-muted alerts are the one muting mechanism that leaks.

**Evidence:** internal/alerts/alerts.go:893-905: `if incID := am.incidentFor(fired[i].alert.DeviceID); incID != 0 { fired[i].alert.IncidentID = &incID; incidentMuted = true ... } am.saveAlert(&fired[i].alert); if !fired[i].alert.Suppressed && !incidentMuted { ... SendAlert ... }` — the row is persisted with Suppressed=false. internal/database/alerts.go:344-349: GetUnacknowledgedAlerts filters `acknowledged = false AND suppressed = false` only. internal/alerts/alerts.go:1347-1354: the escalation loop checks only `alert.PolicyID` and the policy's escalation config — `alert.IncidentID` is never consulted anywhere in CheckEscalations (grep: no IncidentID reference between lines 1296-1423).

**Suggested fix:** Either persist incident-muted alerts with Suppressed=true (they already carry IncidentID for the UI), or have CheckEscalations skip alerts whose IncidentID points at a still-open incident.

### LC-12 [MEDIUM] (server) — CheckDeviceOffline marks the alert active before checking AlertEnabled — disabled DEVICE_OFFLINE policy still produces 'back online' notifications and _RESOLVED rows

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:963` · Dimension: alerting,concurrency-state

Every other state-alert path gates on resolved.AlertEnabled BEFORE recording cooldown/active state (CheckSystemStatus line 140 `if !resolved.AlertEnabled { continue }`; CheckInterfaceStatus 232-234; CheckVPNStatus 920-922; CheckProbeDataFlow 1227 `canSend := resolved.AlertEnabled && am.canAlertWithCooldown(...)`). CheckDeviceOffline is the diverged sibling: it computes canSend from the cooldown alone, calls recordCooldownLocked+markActiveLocked when canSend, and only afterwards returns on `!resolved.AlertEnabled`. So when DEVICE_OFFLINE alerts are disabled by policy/device config, an offline device correctly sends nothing — but activeAlerts[key] is set true. When the device recovers, CheckDeviceOnline→sendRecovery sees wasActive=true and both persists a DEVICE_OFFLINE_RESOLVED alert row and SENDS a 'Device X is back online' notification (alerts.go:832-854) for an alert type the operator explicitly disabled. Net observable behavior: disable DEVICE_OFFLINE alerting and you stop getting DOWN notifications but keep getting UP notifications on every flap. The stale in-memory cooldown also delays the first real alert after re-enabling the rule.

**Evidence:** internal/alerts/alerts.go:963-971: `canSend := am.canAlertWithCooldown(key, now, cooldown); if canSend { am.recordCooldownLocked(key, now, cooldown); am.markActiveLocked(key, now) } am.mu.Unlock(); if !canSend || !resolved.AlertEnabled { return nil }` — active marked before the enabled check. Contrast alerts.go:1227 (CheckProbeDataFlow): `canSend := resolved.AlertEnabled && am.canAlertWithCooldown(key, now, cooldown)`. Recovery side alerts.go:774-854: `wasActive := am.activeAlerts[key]` ... `if !wasActive { return }` ... `am.saveAlert(&alert); ... am.notifier.SendAlert(&alert, nc)` — no AlertEnabled re-check.

**Suggested fix:** Mirror CheckProbeDataFlow: `canSend := resolved.AlertEnabled && am.canAlertWithCooldown(...)` in CheckDeviceOffline (and move the cooldown-record in ProcessTrap/ProcessSyslog/ProcessFlowDetection after the AlertEnabled check for consistency).

### LC-13 [MEDIUM] (server) — Recovery notifications ignore maintenance windows on all paths except system metrics — 'back up' emails fire during maintenance for suppressed 'down' alerts

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:196` · Dimension: alerting

The fire path honors maintenance everywhere (alerts save Suppressed=resolved.InMaintenance and dispatchFired/direct sends skip notification when Suppressed). The recovery path is inconsistent: CheckSystemStatus explicitly skips recovery while InMaintenance (line 197-199), but the sibling recovery paths — interface up (263-269), VPN up (946-952), trap LINK_UP (300-304), CheckDeviceOnline (1013-1019), probe-lag clear (1212-1215) — call sendRecovery with no maintenance check, and sendRecovery notifies whenever the key was active in-process, without checking maintenance or whether the original fire was suppressed. Concrete outcome during a planned window: an interface/VPN/device goes down (alert saved Suppressed=true, markActiveLocked called, NO notification — correct) and comes back up minutes later — the operator gets a 'VPN tunnel X is back up' / 'Device X is back online' notification at 3am with no preceding down notification, one per flap, exactly what the maintenance window was created to prevent. Meanwhile the system-metric sibling has the opposite quirk: a CPU alert cannot auto-resolve at all until the window ends. Two divergent behaviors for the same T2 maintenance feature.

**Evidence:** internal/alerts/alerts.go:196-199 (system metrics only): `for _, rc := range recoveryChecks { if rc.resolved.InMaintenance { continue }`. alerts.go:263-269 (interface): `if iface.Status == "up" { ... am.sendRecovery(key, "INTERFACE_DOWN", ...) }` — no maintenance check; same shape at 946-952 (VPN), 300-304 (trap LINK_UP), 1013-1016 (CheckDeviceOnline). Fire-side suppression: alerts.go:251/934/982 `Suppressed: resolved.InMaintenance` and dispatchFired 900 `if !fired[i].alert.Suppressed && !incidentMuted`. sendRecovery alerts.go:832-854: `if !wasActive { return } ... am.saveAlert(&alert); if flapping { return }; am.notifier.SendAlert(&alert, nc)` — flapping mutes the recovery pair (F13, line 849) but maintenance does not.

**Suggested fix:** Apply the F13 pattern to maintenance: in sendRecovery (or its callers) resolve the config and skip the notification — but still perform the DB auto-resolve — when the device/type is InMaintenance; drop the system-metrics `continue` so resolution isn't deferred.

### LC-15 [MEDIUM] (server) — API tokens survive account disable/delete/demotion — session revocation and token revocation diverged

**Status:** OPEN

**Location:** `internal/api/middleware/middleware.go:262` · Dimension: rbac-authz,mfa-auth-flow

Tranche 1 built two credential types onto one RBAC ladder but only one participates in revocation. Disabling a user, deleting a user, or demoting their role bumps token_version, which instantly kills their JWT sessions — but the API tokens they created keep authenticating forever. AdminAuth's bearer path checks only the token row's own RevokedAt/ExpiresAt and never the state (or existence) of the account behind tok.CreatedByID. An offboarded admin's 'admin'-scope fwm_ token therefore retains full admin rights (settings, user management, minting-blocked-but-everything-else) after the account is disabled or deleted, and a demoted admin's old admin-scope token silently outranks their new viewer role. This contradicts the design's own stated semantics on both sides.

**Evidence:** middleware.go:262-268 (only token-local checks): `if err != nil || tok == nil || tok.RevokedAt != nil || (tok.ExpiresAt != nil && time.Now().After(*tok.ExpiresAt)) { ... 401 }` then `c.Set("role", APITokenScopeRole(tok.Scope))` (line 272) — tok.CreatedByID is set on context (line 270) but never validated. internal/database/api_tokens.go:42-47 doc: 'Validity (expiry/revocation) is the caller's decision — this is pure resolution.' Contrast internal/database/sites_probes.go:165-174: `// SetAdminDisabled flips the disabled flag; disabling also bumps the token version to kill live sessions immediately` (`"token_version": gorm.Expr("token_version + 1")`), and sites_probes.go:197-199 `DeleteAdmin` is a bare `d.db.Delete(&models.Admin{}, id)`. internal/api/handlers/handlers_users.go:205/221/254 (UpdateAdminRole / SetAdminDisabled / DeleteAdmin) — no path touches api_tokens; grep for any revoke-tokens-for-user function found none (api_tokens.go has only RevokeAPIToken by token id). CHANGELOG v0.11.0 entry claims 'Role changes and disables revoke the target's live sessions instantly' — true for sessions only.

**Suggested fix:** In AdminAuth's bearer branch, resolve tok.CreatedByID to the admins row and reject when the account is missing or disabled (fail closed, matching the JWT token-version philosophy); additionally cap a token's effective role at the creator's current role, or cascade-revoke a user's tokens in SetAdminDisabled/DeleteAdmin/UpdateAdminRole.

### LC-16 [MEDIUM] (server) — Operator probe-onboarding dead-end: CreateProbe is operator-level but the only key reveal (regenerate-key) is admin-only; deploy modal shows the redaction mask '********' as the key

**Status:** OPEN

**Location:** `cmd/api/main.go:672` · Dimension: rbac-authz,rbac-authz,rbac-authz

roles.go declares 'probes lifecycle' as operator territory, and POST /admin/api/probes, approve/reject/decommission/recommission are all operator-reachable. But since AUDIT-017 the plaintext registration key is generated at create, hashed, and DISCARDED — 'RegenerateProbeKey is the show-once reveal path' — and /admin/api/probes/:id/regenerate-key is in adminOnlyRoutes. So an operator can create and even approve a probe but can never obtain the key the collector needs to register: a state with no exit at operator level. The UI makes it worse for everyone: CreateProbe redacts RegistrationKey to the mask '********' (truthy), so admin-probes.js's fallback '(no key — click Regenerate Key)' is unreachable and the auto-opened deploy instructions print PROBE_REGISTRATION_KEY=******** as if it were a real key.

**Evidence:** main.go:662-673 adminOnlyRoutes contains `"/admin/api/probes/:id/regenerate-key": true` but NOT `/admin/api/probes`; main.go:782 `admin.POST("/api/probes", handler.CreateProbe)` and main.go:789 `admin.POST("/api/probes/:id/approve", handler.ApproveProbe)` fall to the operator mutation default (middleware.go:360). internal/api/handlers/handlers_probes.go:115-124: plaintext `hex.EncodeToString(keyBytes)` is hashed into probe.RegistrationKey and never returned; line 147 `httputil.RedactProbe(&probe)`. internal/httputil/redact.go:11 `const RedactedMask = "********"`, :38-40 masks non-empty keys. cmd/api/static/js/admin-probes.js:264 `var key = probe.registration_key || '(no key — click Regenerate Key)'` — mask is truthy so fallback never fires; :270 `'\nPROBE_REGISTRATION_KEY=' + (probe.registration_key || 'MISSING')`; :335 regenerate calls `/probes/' + probeId + '/regenerate-key'` which 403s for operator. internal/auth/roles.go:10-13: operator = 'day-to-day mutations (devices, alerts, sites, probes lifecycle)'.

**Suggested fix:** Either move POST /admin/api/probes into adminOnlyRoutes (it mints credential material, consistent with the map's stated intent) or drop regenerate-key to operator; separately, have RedactProbe clear (not mask) registration_key on the create/GET responses so the UI's 'click Regenerate Key' hint actually renders.

### LC-17 [MEDIUM] (server) — Copy-paste sibling diverged after RBAC: test-webhook/test-email are admin-only but the identical-shape POST /admin/api/irc/servers/test (and all IRC credential mutations) are operator-level

**Status:** OPEN

**Location:** `cmd/api/main.go:920` · Dimension: rbac-authz

The adminOnlyRoutes rationale is 'settings, users, or credential material', and the notification-channel test endpoints /admin/api/settings/test-email and /test-webhook were listed. The IRC subsystem is the same feature class — a notification channel whose config carries server/NickServ/SASL/oper passwords and whose test endpoint makes the server open an outbound connection to an arbitrary user-supplied host:port with credentials from the request body — yet every IRC route (create/update servers and channels with passwords, connect/disconnect, send, and servers/test) fell to the operator mutation default. This directly contradicts the role ladder's own definition that operator must 'not touch settings, users, tokens, or other credential material', and it means the RBAC boundary differs between two sibling notification channels for no documented reason.

**Evidence:** main.go:664-665 adminOnlyRoutes: `"/admin/api/settings/test-email": true, "/admin/api/settings/test-webhook": true`; main.go:915-932: `admin.POST("/api/irc/servers", handler.CreateIRCServer)` … `admin.POST("/api/irc/servers/test", handler.TestIRCServer)` — none present in the adminOnlyRoutes map (main.go:662-673), so RequireRole's default `required = auth.RoleOperator` applies (middleware.go:360). handlers_irc.go:460-492: test request struct has `Password string`, `SASLPassword string` plus arbitrary `ServerHost`/`ServerPort`, and `irc.NewTestBot(req.ServerHost, req.ServerPort, …, req.Password, …)` connects outbound. internal/models/models.go:1302-1309: IRCServer stores `NickServPassword`, `ServerPassword`, `SASLPassword`. internal/auth/roles.go:11-13: 'RoleOperator can do day-to-day mutations … but not touch settings, users, tokens, or other credential material.'

**Suggested fix:** Add /admin/api/irc/servers, /admin/api/irc/servers/:id, /admin/api/irc/servers/test, and /admin/api/irc/channels(+/:id) to adminOnlyRoutes (or at minimum the /test endpoint, to match its test-webhook sibling), and extend the RBAC tests to cover the IRC route family.

### LC-19 [MEDIUM] (server) — Partition conversion silently drops the AUDIT-034 flow_samples src/dst indexes (and other model-tag indexes) on every fresh PG install; per-partition recreation list never got them

**Status:** OPEN

**Location:** `internal/database/migrate.go:443` · Dimension: db-migrations

Migration v2 (migratePartitionHighVolume, within the audit window: v0.10.380) converts empty high-volume tables to partitioned parents via `CREATE TABLE %s (LIKE %s_prepart INCLUDING DEFAULTS) PARTITION BY RANGE (%s)` — INCLUDING DEFAULTS only, so ALL indexes created by the v1 baseline AutoMigrate from GORM struct tags are destroyed when the renamed `_prepart` table is dropped. EnsurePartitions then recreates only (device_id, timestamp) + (timestamp) per child partition, plus special cases for syslog_messages/trap_events severity and the interface_stats 3-col index (migrate.go:311-348). Nothing ever recreates flow_samples' `idx_flow_src_addr`/`idx_flow_dst_addr` (the explicit AUDIT-034 fix, shipped v0.10.318 — BEFORE partitioning v0.10.380) or the probe_id indexes on flow_samples/syslog_messages/trap_events, or syslog_summaries' interval_type/severity indexes. On PG, AutoMigrate for these models runs exactly once (baseline v1); every later flow migration (v9-v15, v29) gates AutoMigrate to `!IsPostgres()`. Consequence: on every fresh Postgres install (which auto-converts because the tables are empty), the connection flow-stats queries `src_addr LIKE ? / dst_addr LIKE ?` (connection_detail.go:955) full-scan flow_samples partitions — exactly the regression AUDIT-034 was filed to fix — and Tranche 3 NetFlow/IPFIX (full-flow export, not sampled) multiplies the row volume being scanned. The regression test TestFlowSampleIndexes_AUDIT034 only runs on the SQLite test backend (NewDatabaseForTesting), so it can never catch this. Populated prod tables skip conversion and keep their indexes, which is why prod hasn't surfaced it.

**Evidence:** migrate.go:443: `fmt.Sprintf(`CREATE TABLE %s (LIKE %s_prepart INCLUDING DEFAULTS) PARTITION BY RANGE (%s)`, table, table, col)` — no INCLUDING INDEXES. migrate.go:311-318 (EnsurePartitions per-partition index list): only `{idx_%s_device_ts, (device_id, timestamp)}` and `{idx_%s_timestamp, (timestamp)}` plus severity/interface_stats special cases. models.go:877-878: `SrcAddr string `json:"src_addr" gorm:"index:idx_flow_src_addr"`` / `DstAddr ... idx_flow_dst_addr` — created only by AutoMigrate. docs/AUDIT.md:1299 records AUDIT-034 as resolved by 'gorm struct tags, created by AutoMigrate'. migrate.go:1121-1123 / 1330-1331 show all PG-side flow migrations skip AutoMigrate (`if !d.dialect.IsPostgres() { return d.db.AutoMigrate(...) }`), so nothing re-creates the tag indexes after conversion. flowindex_audit034_test.go:14: `db := NewDatabaseForTesting(t)` (SQLite only). connection_detail.go:955: `"(src_addr LIKE ? ESCAPE '\\' AND dst_addr LIKE ? ESCAPE '\\')"` is still the hot query.

**Suggested fix:** Either use `INCLUDING ALL`/`INCLUDING INDEXES` in convertEmptyTableToPartitioned (parent-level indexes propagate to future children automatically on PG11+), or extend the EnsurePartitions per-table index list with the model-tag indexes that the tags declare (flow_samples src_addr/dst_addr/probe_id, syslog_summaries interval_type/severity, probe_id on syslog_messages/trap_events). Add a PG integration test that asserts pg_indexes contains idx_flow_src_addr after the partition migration.

### LC-22 [MEDIUM] (server) — T2 incidents table (migration v27) has no retention path, and an incident open when its device is deleted is a permanent dead-end state

**Status:** OPEN

**Location:** `internal/database/incidents.go:12` · Dimension: retention-cleanup

The incidents table shipped in Tranche 2 (migrations.go:78, v27) with no cleanup: CleanupOldData has no entry for it, there is no RETENTION_INCIDENT_DAYS knob in config.go, and docs/DATA-RETENTION.md doesn't mention it. Rows accumulate forever on flapping networks (one row per device outage). Worse, the only code path that resolves an incident is device recovery (incidents_f12.go:83 ResolveIncident, called from closeIncident on recovery). DeleteDevice (devices.go:165-172) deletes only device_connections + the device row — correct for telemetry preservation, but it leaves the device's OPEN incident unresolvable forever: refreshOpenIncidents (incidents_f12.go:31-47) reloads every open incident into the poller's cache each cycle, and the incidents UI shows a permanently-open incident for a device that no longer exists. Meanwhile the alerts attached to it are aged out at 90 days by AUDIT-031 cleanup, so the open incident's alert list empties while the incident itself never closes.

**Evidence:** internal/database/migrations.go:78 `{version: 27, name: "incidents", ...}`; grep of internal/database/cleanup.go for Incident/incidents: zero hits; internal/config/config.go:352-380 has no incident retention field. Only resolve path: internal/alerts/incidents_f12.go:83 `inc, err := am.db.ResolveIncident(device.ID)` inside closeIncident (device recovery). internal/database/devices.go:165-171 `func (d *Database) DeleteDevice(id uint) error { ... Delete(&models.DeviceConnection{}) ... return tx.Delete(&models.Device{}, id).Error }` — no incident resolution. incidents_f12.go:35-46 reloads all GetOpenIncidents() into the cache every poll cycle.

**Suggested fix:** Resolve (or mark orphaned) the device's open incident inside DeleteDevice, and add a resolved-incidents retention entry (e.g. RETENTION_INCIDENT_DAYS defaulting alongside alert retention) to CleanupOldData; document in DATA-RETENTION.md.

### LC-26 [MEDIUM] (server) — PROBE_DATA_TRUNCATED alert promised by the M1 truncation fix can never fire: RecordProbeDataTruncation reads a lastAlert key that nothing ever writes, and its 5-minute guard is inverted

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:1275` · Dimension: time-units-math,concurrency-state

The M1 fix (v0.10.538, in the audit window) wired truncateProbeBatch to 'record an operator-visible probe alert when the overshoot exceeds 20%' (handlers_data.go:61-63,74-75). It calls the old RecordProbeDataTruncation (v0.10.155), which reads `lastTruncation := am.lastAlert[key]` for key `probe_truncation_<id>` and returns early when `lastTruncation.IsZero() || now.Sub(lastTruncation) > 5*time.Minute`. Two compounding bugs: (1) the ONLY writer of am.lastAlert is recordCooldownLocked (alerts.go:565), which this function never calls and no other path uses with a probe_truncation key — so lastTruncation is ALWAYS the zero time and the function ALWAYS returns before building the alert; (2) even if the timestamp were recorded, the time comparison is inverted relative to the comment 'Only alert if truncation happened recently (within 5 minutes) to avoid spam' — as an anti-spam guard it should suppress when the previous alert is RECENT, but as written it suppresses when the previous event is old or absent. Net effect: batch tails are still dropped with only a server-side log line; the operator-visible alert the 2026-07-01 M1 resolution documents does not exist. No test covers PROBE_DATA_TRUNCATED.

**Evidence:** alerts.go:1270-1277: `am.mu.Lock()\n\tlastTruncation := am.lastAlert[key]\n\tam.mu.Unlock()\n\n\t// Only alert if truncation happened recently (within 5 minutes) to avoid spam\n\tif lastTruncation.IsZero() || now.Sub(lastTruncation) > 5*time.Minute {\n\t\treturn\n\t}` — nothing in the function writes am.lastAlert[key]; the sole writer is alerts.go:565 `am.lastAlert[key] = now` inside recordCooldownLocked, never invoked with a probe_truncation key. Caller: internal/api/handlers/handlers_data.go:74-75 `if h.alertManager != nil && orig > capN+capN/5 {\n\t\th.alertManager.RecordProbeDataTruncation(probe.ID, probe.Name, orig, capN)` with the comment (line 61-63) claiming 'an operator-visible probe alert is recorded when the overshoot exceeds 20%'.

**Suggested fix:** Record the truncation time (am.lastAlert[key] = now via recordCooldownLocked) and invert the guard: return only when a PREVIOUS truncation alert was sent within the cooldown; then save+send the alert. Add a regression test that a single truncation produces one PROBE_DATA_TRUNCATED alert.

### LC-27 [MEDIUM] (server) — F05 MTTA/MTTR counts F12 INCIDENT_RESOLVED summary rows as zero-minute responses: the 'recovery' companion-row exclusion was not extended to the 'incident' companion rows shipped one version earlier

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/database/alerts.go:372` · Dimension: time-units-math,sibling-divergence

GetAlertResponseStats (F05, v0.11.10) excludes synthetic companion rows only via `metric_name <> 'recovery'`. But F12 incident grouping (v0.11.9) creates a second kind of synthetic companion row: closeIncident saves an INCIDENT_RESOLVED summary with MetricName="incident", Timestamp=now, AcknowledgedAt=&now, ResolvedAt=&now and empty Notes (incidents_f12.go:95-107). That row passes every F05 filter (metric_name != 'recovery'; acknowledged_at NOT NULL; Notes lacks the "Auto-resolved:" prefix that gates MTTA), so each closed incident injects one 0-minute sample into BOTH the MTTA and MTTR averages. On an outage-heavy network where incidents are common, the report's headline 'Mean time to acknowledge/resolve' is biased optimistically toward zero — the exact storm scenarios F12 targets are the ones that corrupt the F05 numbers most. GetNoisiestAlerts (alerts.go:409) shares the same single-exclusion pattern, so each incident also adds a phantom INCIDENT_RESOLVED row to the noise leaderboard counts. Classic sibling divergence between two features shipped a week apart: the author of F05 knew to exclude the OLD companion row type (sendRecovery's metric_name="recovery", which has the identical instant-ack shape, alerts.go:837-847) but missed the NEW one.

**Evidence:** database/alerts.go:371-373: `Where("timestamp > ? AND metric_name <> ? AND (acknowledged_at IS NOT NULL OR resolved_at IS NOT NULL)", cutoff, "recovery")` and 378-383: `auto := strings.HasPrefix(rows[i].Notes, "Auto-resolved:")\n\t\tif rows[i].AcknowledgedAt != nil && !auto {\n\t\t\tif dur := rows[i].AcknowledgedAt.Sub(rows[i].Timestamp); dur >= 0 {\n\t\t\t\tackSum += dur.Minutes(); ackedCount++` — vs internal/alerts/incidents_f12.go:96-107: `summary := models.Alert{\n\t\tTimestamp: now, ... AlertType: "INCIDENT_RESOLVED", ... MetricName: "incident",\n\t\tAcknowledged: true,\n\t\tAcknowledgedAt: &now,\n\t\tResolvedAt: &now,\n\t}` (dur = now-now = 0, Notes empty so not excluded from MTTA).

**Suggested fix:** Extend the exclusion to all synthetic companion rows: `metric_name NOT IN ('recovery','incident')` (or filter alert_type suffixes '_RESOLVED') in both GetAlertResponseStats and GetNoisiestAlerts.

### LC-31 [MEDIUM] (server) — Policy editor's hard-coded 12-type ALERT_TYPES list silently deletes rules for the ~14 server alert types it doesn't render on every UI save

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-main.js:3132` · Dimension: frontend-backend

The alert-policy modal renders and collects rules only for a fixed 12-entry list: `var ALERT_TYPES = ['CPU_HIGH','MEMORY_HIGH','DISK_HIGH','SESSIONS_HIGH','INTERFACE_DOWN','INTERFACE_ERRORS','VPN_TUNNEL_DOWN','DEVICE_OFFLINE','TRAFFIC_SPIKE','SYSLOG_EMERGENCY','SYSLOG_ALERT','SYSLOG_CRITICAL']` (admin-main.js:3132). The server's AlertType enum (internal/models/models.go:15-45) additionally defines CONFIG_CHANGE, SSH_HOST_KEY_CHANGED, PROBE_DATA_LAG, PROBE_DATA_TRUNCATED, HA_HEARTBEAT_FAIL, HA_MEMBER_DOWN, HA_MEMBER_UP, HA_STATE_CHANGE, HA_SWITCH, SFLOW_AGENT_DROPS, SFLOW_CLEARTEXT, SFLOW_UNEXPECTED_EGRESS, SFLOW_SAMPLING_BACKOFF, SFLOW_CAPACITY, TEST_ALERT — and the resolver (internal/alerts/policy.go:185-196) honors per-type rules for ALL of them (severity, enable/disable, hysteresis clear_threshold, zscore mode — the T2 features). Two consequences: (a) none of those types can be tuned per-policy from the UI at all; (b) worse, saving a policy from the modal does `PUT /alert-policies/:id/rules` with ONLY the 12 rendered rows (collectRules, admin-main.js:3315-3352 → 3392), and the server implements that as delete-all-then-insert (internal/database/alerts.go:73-83: `Delete(&models.AlertRule{}).Where("policy_id = ?")` then Create). Any rule that exists for a non-rendered type (created via the API, or carried over by CloneAlertPolicy which copies ALL src rules, handlers_alert_policies.go:171-182) is silently destroyed the next time anyone touches the policy in the SPA.

**Evidence:** JS: admin-main.js:3132 `var ALERT_TYPES = ['CPU_HIGH',...,'SYSLOG_CRITICAL'];` (12 entries); admin-main.js:3316 `var rows = document.querySelectorAll('#policy-rules-body tr');` — collectRules only walks rendered rows; admin-main.js:3392 `return apiFetch(API_BASE + '/alert-policies/' + policyId + '/rules', {method: 'PUT', body: rules});`. Server: internal/models/models.go:15-45 defines 26+ AlertType constants incl. `AlertTypeHAMemberDown AlertType = "HA_MEMBER_DOWN"`, `AlertTypeSFlowCleartext AlertType = "SFLOW_CLEARTEXT"` with comment 'these consts document the set and back defaultSeverityForType / policy rules'. internal/database/alerts.go:73-76 `// Delete existing rules for this policy / if err := d.db.Where("policy_id = ?", policyID).Delete(&models.AlertRule{}).Error` — full replace, so unsent types are wiped. internal/alerts/policy.go:188-190 `if policy.Rules[i].AlertType == alertType { rule = &policy.Rules[i] }` — rules matter for every type.

**Suggested fix:** Either derive the rules table from the server (expose the AlertType list via an endpoint or embed the full list in JS), or make BatchUpsertAlertRules merge instead of replace (preserve rules for alert_types absent from the payload). At minimum extend ALERT_TYPES to the full enum so T2 hysteresis/zscore is configurable for HA/sFlow/config-change types.

### LC-32 [MEDIUM] (server) — Snoozed alerts are unreachable and un-unsnoozeable in the UI: server hides them by default and no JS ever sends include_snoozed (the 'Show snoozed' toggle referenced in comments doesn't exist)

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-main.js:1881` · Dimension: frontend-backend

GET /admin/api/alerts filters out snoozed rows unless `include_snoozed=true` (handlers_analytics.go:99-101). No frontend code ever sends that param: buildAlertParams (admin-main.js:1881-1894) builds only limit/hours/device_id/alert_type/severity/acknowledged, `grep -rn include_snoozed cmd/api/static/js web/` returns nothing, and admin.html contains zero occurrences of 'snooz' (no toggle control). Yet the JS renders a SNOOZED badge (admin-main.js:1938) whose own comment claims 'Snoozed rows are only visible if the operator opted in via the "Show snoozed" toggle' — that toggle was never shipped (git log -S include_snoozed shows only the v0.10.218 server commit). The only Unsnooze control lives in the alert-detail modal (admin-main.js:1991), which is reachable solely by clicking a row in the alerts list — a list that can never contain a snoozed alert. Net effect: snoozing (up to the 720h/30-day cap, handlers_analytics.go:721-722) makes an alert vanish from every admin view with NO way to see it or reverse it until the snooze expires — a state-machine dead-end, dangerous when an operator fat-fingers 720 instead of 72 hours on a critical alert. The 'select all matching' bulk-ack also silently skips snoozed rows for the same reason.

**Evidence:** Server default: internal/api/handlers/handlers_analytics.go:99-101 `if c.Query("include_snoozed") != "true" { q = q.Where("snoozed_until IS NULL OR snoozed_until < ?", time.Now()) }`. JS never sends it: admin-main.js:1881-1894 buildAlertParams pushes only limit/hours/device_id/alert_type/severity/acknowledged. Stale comment: admin-main.js:1926-1928 `// Snoozed rows are only visible if the operator opted in via the "Show snoozed" toggle (the server default filters them out).` — `grep -n snooz web/admin/admin.html` = no matches. Dead control: admin-main.js:1991 `'<button ... data-action="unsnooze-alert" ... >Unsnooze</button>'` only rendered inside showAlertDetail, which is only opened from list rows.

**Suggested fix:** Add the missing 'Show snoozed' option (e.g. a third value in #alerts-filter-ack or a checkbox) that appends include_snoozed=true to buildAlertParams; the badge/Unsnooze code paths already handle the rendering.

### LC-34 [MEDIUM] (server) — Maintenance-window Edit round-trip shifts start/end by the browser's UTC offset: create path converts to local time, edit path slices the UTC ISO string into a datetime-local input

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-main.js:3615` · Dimension: frontend-backend

showMaintModal's create path correctly converts now/now+2h to local wall time before filling the datetime-local inputs (admin-main.js:3568 `toLocal` subtracts getTimezoneOffset). The edit path instead does `w.start_time.slice(0, 16)` (admin-main.js:3615-3616). The server marshals StartTime/EndTime as Go time.Time → RFC3339 UTC (e.g. "2026-07-04T20:00:00Z", models.go:427-428; prod runs UTC), so the slice yields the UTC wall-clock string, which a datetime-local input interprets as LOCAL time. On save, the form does `new Date(value).toISOString()` (admin-main.js:3661-3662), treating that UTC wall time as local and re-converting — so merely opening a window in Edit and clicking Save shifts both timestamps by the timezone offset (e.g. -4h in EDT), every single edit. Repeated edits walk the suppression window further and further from what the operator scheduled, meaning alerts get suppressed at the wrong hours (or the window silently 'expires' early). The two sibling code paths cannot both be correct in any non-UTC browser.

**Evidence:** Create (correct): admin-main.js:3568 `function toLocal(d) { return new Date(d.getTime() - d.getTimezoneOffset() * 60000).toISOString().slice(0, 16); }`. Edit (wrong): admin-main.js:3615-3616 `if (w.start_time) document.getElementById('maint-start').value = w.start_time.slice(0, 16);` — w.start_time is the marshaled `StartTime time.Time` `json:"start_time"` (internal/models/models.go:427), i.e. an RFC3339 UTC string. Save: admin-main.js:3661-3662 `start_time: new Date(document.getElementById('maint-start').value).toISOString()` — datetime-local values parse as LOCAL, completing the shift.

**Suggested fix:** In the edit prefill, parse the ISO string and reuse toLocal(): `document.getElementById('maint-start').value = toLocal(new Date(w.start_time));` (same for end_time).

### LC-36 [MEDIUM] (server) — Flows raw-samples list and CSV export ignore the page's time-range filter: JS claims and labels an hours-bounded export, but GET /admin/api/flows has no hours/time parameter at all

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-flows.js:482` · Dimension: frontend-backend

The Flows page has one shared filter row whose range pills set state.hours; the stats endpoint honors it (statsURL, admin-flows.js:466 sends hours; GetFlowStats parses it via ParseHours). But samplesURL (admin-flows.js:482-498) never includes hours, and GetFlowSamples (handlers_analytics.go:249-334) has no time-window filter of any kind — it is purely newest-first limit/offset. The CSV export is where this becomes an actual lie to the operator: its comment says it 'Pulls every flow row that matches the current filter state' (admin-flows.js:999-1004) and filterSignature() stamps the hours into the filename ('flows-…-24h-…csv', admin-flows.js:1082 `if (state.hours) bits.push(state.hours + 'h')`), yet the file actually contains the newest ≤10000 rows regardless of range — with a 1h range selected the export can span days; with a 168h range it can cover far less. The exported evidence file is mislabeled, which matters for the incident-forensics use this feature targets.

**Evidence:** JS stats sends hours: admin-flows.js:466 `var params = ['hours=' + encodeURIComponent(state.hours)];` (statsURL). JS samples does not: admin-flows.js:482-484 `function samplesURL(limit, offset) { var p = ['limit=' + limit]; if (offset > 0) p.push('offset=' + offset); ...` — no hours key anywhere in the function. Export claim + filename: admin-flows.js:999 `* Pulls every flow row that matches the current filter state` and admin-flows.js:1082 `if (state.hours) bits.push(state.hours + 'h');`. Server: internal/api/handlers/handlers_analytics.go:249-334 GetFlowSamples — filters on probe_id/device_id/site_id/src/dst/dst_port/protocol/app_category/direction/dst_country/dst_asn/flow_source only; no c.Query("hours"), no timestamp Where.

**Suggested fix:** Add an optional hours param to GetFlowSamples (`timestamp > now()-hours`, same ParseHours as the stats sibling) and send it from samplesURL; or stop putting hours in the CSV filename/claim.

### LC-38 [MEDIUM] (server) — webhook_secret / pagerduty_routing_key / opsgenie_api_key are stored plaintext, contradicting the code's own encrypted-at-rest contract

**Status:** OPEN

**Location:** `internal/api/handlers/handlers_settings.go:297` · Dimension: config-settings

settingsSecretKeys is documented as 'the source of truth for which system_settings rows hold secret values that must be encrypted at rest' (handlers_settings.go:26-29), and the inline AUDIT-026 comment claims 'a future field added to BOTH [allowedKeys and secretKeys] is encrypted' (handlers_settings.go:290-293). But the actual `h.db.EncryptField(s.Value)` call sits INSIDE `case "smtp_password":`, so it is unreachable for the three T2-5 secret keys added to both maps in v0.11.x — they are persisted as plaintext. The read side was written assuming encryption: alerts.go:712-714 comments 'Encrypted at rest like smtp_password; DecryptField is idempotent for plaintext' — the idempotency masks the gap so nothing visibly breaks, meaning the encryption silently never happens for incident-channel credentials while SMTP passwords are AES-GCM encrypted. The guard test handlers_settings_audit026_test.go only checks map MEMBERSHIP, not that encryption executes, so it passes despite the contract being broken.

**Evidence:** handlers_settings.go:296-299 (inside `case "smtp_password":` which begins at 261): `if secretKeys[s.Key] && s.Value != "" && h.db != nil {\n  s.Value = h.db.EncryptField(s.Value)\n}` — there is no case for webhook_secret/pagerduty_routing_key/opsgenie_api_key in the switch (cases cover thresholds, spike_*, detect_*, report_*, booleans, smtp_* fields, display_timezone, public_interfaces only), so these keys never reach EncryptField. Contract comment at handlers_settings.go:288-293: 'a future field added to allowedKeys but NOT to secretKeys is stored as plaintext (the right default) and a future field added to BOTH is encrypted' — false for fields added to both. Read-side expectation at internal/alerts/alerts.go:712-714: `case "webhook_secret":\n  // Encrypted at rest like smtp_password; DecryptField is idempotent\n  // for plaintext (env-sourced) values.`

**Suggested fix:** Move the `if secretKeys[s.Key] && s.Value != ""` EncryptField block out of the smtp_password case to run after the switch for every valid setting (it is already correctly gated on secretKeys). Add a behavioral test asserting the stored row for each settingsSecretKeys key carries the {enc} prefix.

### LC-39 [MEDIUM] (server) — Env-configured T2 incident channels (PAGERDUTY_ROUTING_KEY / OPSGENIE_API_KEY / TEAMS_WEBHOOK_URL / WEBHOOK_SECRET) deliver real alerts but the Test buttons report 'not configured' — getNotificationSetting fallback never extended

**Status:** OPEN

**Location:** `internal/api/handlers/handlers_settings.go:368` · Dimension: config-settings

The same knob is configurable in two places with divergent read paths. Real alert delivery reads config seeded from env (internal/config/config.go:449-451 loads PAGERDUTY_ROUTING_KEY, OPSGENIE_API_KEY, TEAMS_WEBHOOK_URL; :431 WEBHOOK_SECRET) into AlertManager/notifier (notifier.go SnapshotConfig copies PagerDutyRoutingKey/OpsgenieAPIKey/TeamsWebhookURL/WebhookSecret). But the API's getNotificationSetting env-fallback switch (handlers_settings.go:368-387) was never extended for Tranche 2 — it only lists smtp_*, slack_webhook, discord_webhook, webhook_url. So an operator who configures the incident channels purely via env gets working production notifications while TestWebhook returns 'No PagerDuty routing key configured' (line 814-816) / 'No Opsgenie API key configured' (820-823), the teams_webhook test resolves to "" and fails with 'No webhook URL configured' (834-836), and the F18 test-signing path skips signing (line 906 `if secret := h.getNotificationSetting("webhook_secret"); secret != ""`) even though production deliveries ARE signed — the exact end-to-end verification the F18 comment promises ('sign the generic-webhook test exactly like production deliveries') is broken for env-configured secrets. Copy-paste sibling divergence between the pre-T2 channel set and the T2 additions.

**Evidence:** handlers_settings.go:367-388 env fallback: `switch key { case "smtp_host": ... case "slack_webhook": return h.config.Alerts.SlackWebhookURL ... case "webhook_url": return h.config.Alerts.WebHookURL }\nreturn ""` — no pagerduty_routing_key / opsgenie_api_key / teams_webhook / webhook_secret cases, though internal/config/config.go:449-451 loads them: `PagerDutyRoutingKey: getEnv("PAGERDUTY_ROUTING_KEY", ""), OpsgenieAPIKey: getEnv("OPSGENIE_API_KEY", ""), TeamsWebhookURL: getEnv("TEAMS_WEBHOOK_URL", "")`. TestWebhook at handlers_settings.go:813-816: `case "pagerduty": if h.getNotificationSetting("pagerduty_routing_key") == "" { c.JSON(http.StatusBadRequest, response.Error("No PagerDuty routing key configured")); return }`. Delivery path uses env-seeded config: internal/notifier/notifier.go:113-116 `WebhookSecret: cfg.WebhookSecret, PagerDutyRoutingKey: cfg.PagerDutyRoutingKey, ...` and internal/notifier/incident_channels.go:73 `return n.PostJSONSigned(pagerDutyEventsURL, buildPagerDutyPayload(alert, nc.PagerDutyRoutingKey), "")`.

**Suggested fix:** Add pagerduty_routing_key, opsgenie_api_key, teams_webhook, webhook_secret cases to getNotificationSetting's env fallback (returning h.config.Alerts.PagerDutyRoutingKey etc.), matching the slack/discord/webhook_url precedent (DB non-empty wins, env fallback otherwise).

### LC-41 [MEDIUM] (collector) — sFlow/NetFlow source-IP allowlist silently never applied when TFTP is disabled (early return couples all receiver allowlists to the TFTP server)

**Status:** OPEN

**Location:** `cmd/collector/main.go:1109` · Dimension: sibling-divergence

applyTFTPAllowlist() is the single 'push the fleet allowlist to every receiver' hook (its own comment at main.go:1131-1133 says so), but it still begins with the pre-M2 guard `if c.tftpServer == nil { return }`. c.tftpServer is only set when PROBE_TFTP_CONFIG_ENABLED=true (main.go:341-346). With TFTP disabled, every call site (startup line 369, sFlow start line 525, NetFlow start line 569, device refresh line 1681) returns before reaching the sFlow/NetFlow SetAllowedSourceIPs blocks, so allowedSrcIPs stays nil = allow-any (sflow.go:30, netflow.go:70). The call-site comments claim 'Deny-all until devices are known', but the receivers actually accept spoofed flow datagrams from any source forever — the exact forgery scenario audit M2 (sFlow) and the Tranche 3 NetFlow comment (main.go:1128-1131) say the allowlist exists to prevent. Classic sibling drift: the sFlow (v1.2.x) and NetFlow (v1.3.0) allowlist blocks were appended to a function whose original TFTP-only early-return no longer matches its widened responsibility.

**Evidence:** cmd/collector/main.go:1108-1111: `func (c *Collector) applyTFTPAllowlist() {\n\tif c.tftpServer == nil {\n\t\treturn\n\t}` — followed at 1123-1126 by `if c.sflowReceiver != nil { c.sflowReceiver.SetAllowedSourceIPs(ips, ...) }` and at 1134-1136 by `if c.netflowReceiver != nil { c.netflowReceiver.SetAllowedSourceIPs(ips, ...) }`. TFTP server only created when enabled: main.go:341-343 `if probeCfg.TFTPConfigEnabled { ... c.startTFTPServer() }`. Receiver default is allow-all: internal/sflow/sflow.go:30 `allowedSrcIPs map[string]bool // nil = allow any (back-compat)` and internal/netflow/netflow.go:70 same. Call-site comment contradicted: main.go:522-525 `// ... without this the receiver would accept any source until the first device refresh. Deny-all until devices are known.\nc.applyTFTPAllowlist()`.

**Suggested fix:** Move the `c.tftpServer == nil` check to guard only the TFTP-specific statements (lines 1115-1117); always run the sFlow/NetFlow SetAllowedSourceIPs blocks. Add a regression test with TFTPConfigEnabled=false asserting the receivers get a non-nil allowlist.

### LC-43 [MEDIUM] (both) — Vendor registry drift: cisco_asa and generic are valid device vendors in the API/UI but have no SNMP profile in either repo — both silently poll FortiGate enterprise OIDs; roadmap doc asserts the server profile exists

**Status:** OPEN

**Location:** `internal/api/handlers/handlers.go:273` · Dimension: sibling-divergence

The four parallel vendor lists have drifted. validVendors (server handlers.go:273-282) and the admin UI dropdown (web/admin/admin.html:1384-1392) accept all 8 vendors including cisco_asa and generic; configdiff registers cisco_asa as a RICH normalizer (vendor_cisco_asa.go:17). But the SNMP VendorProfile registry — in BOTH repos — registers only 6 profiles (fortigate, paloalto, sonicwall, firewalla, pfsense, opnsense); there is no vendor_cisco_asa.go or vendor_generic.go in internal/snmp of either repo. resolveVendor falls back to DefaultVendor() = the FortiGate profile (server snmp.go:198-206, vendor.go:79-90; collector snmp.go:241-248), so a device the operator explicitly configures as cisco_asa or generic is polled with Fortinet enterprise OIDs (.1.3.6.1.4.1.12356...) and has FortiGate TrapOIDs applied to its traps — yielding permanently empty system status/VPN/HA/sensor data with no warning, instead of at least standard MIB-II. docs/FEATURE-ROADMAP.md:18 makes the drift explicit by claiming the SERVER registry includes 'cisco_asa ... generic' and that only the collector lacks cisco_asa, and line 211 plans to 'port the cisco_asa profile to the collector' — a profile that does not exist server-side either.

**Evidence:** internal/api/handlers/handlers.go:273-282: `var validVendors = map[string]bool{ "fortigate": true, "paloalto": true, "cisco_asa": true, "sonicwall": true, "firewalla": true, "pfsense": true, "opnsense": true, "generic": true }`. Registered SNMP profiles (grep RegisterVendor, both repos): only firewalla/paloalto/fortigate/opnsense/pfsense/sonicwall — no cisco_asa, no generic (server internal/snmp/vendor_*.go; collector internal/snmp/vendor_*.go). Fallback: server internal/snmp/snmp.go:198-206 `profile := GetVendorProfile(vendor); if profile == nil { profile = DefaultVendor() }` with vendor.go:79-81 `// DefaultVendor returns the FortiGate vendor profile.` Contradicting doc: docs/FEATURE-ROADMAP.md:18 'Server: fortigate/paloalto/cisco_asa/pfsense/opnsense/sonicwall/firewalla/generic/linux_vpn/bsd_vpn. Collector ships a subset — **no cisco_asa**.'

**Suggested fix:** Add a standard-MIB (HOST-RESOURCES/IF-MIB) 'generic' profile and register it; route cisco_asa to it (or a real ASA profile) instead of the FortiGate fallback; fix FEATURE-ROADMAP.md:18/211; add a test asserting every validVendors entry resolves to a non-FortiGate-fallback profile or is explicitly documented.

### LC-45 [MEDIUM] (collector) — Collector docker-compose.yml pins image :1.2 while configuring 1.3.0-only NetFlow features — documented deploy path silently ships a collector without the NetFlow receiver

**Status:** OPEN

**Location:** `docker-compose.yml:3` · Dimension: dead-stale

The collector released v1.3.0 (NetFlow v5/v9 + IPFIX receiver) but the committed docker-compose.yml still pulls the moving major.minor tag `:1.2`. The Docker publish workflow generates `{{major}}.{{minor}}` tags from the source version const, so `:1.2` is frozen at the last 1.2.x image forever and will never receive the NetFlow code. The SAME compose file sets `PROBE_NETFLOW_PORT`, `PROBE_IPFIX_PORT`, `PROBE_NETFLOW_ENABLED`, and `PROBE_FLOW_DEDUP` — env vars a 1.2.x binary silently ignores. An operator following the shipped compose (which README calls the default: ':1.2 ... default in docker-compose.yml', README.md:286) deploys Tranche 3, points firewalls at UDP/2055-4739, and gets no flows with no error. README tag guidance (lines 128, 199, 286) and the ':1.2.108' pin example are equally stale.

**Evidence:** docker-compose.yml:3 `image: xphox/firewall-collector:1.2`; docker-compose.yml:68-69 `PROBE_NETFLOW_PORT: "2055"` / `PROBE_IPFIX_PORT: "4739"`, :82 `PROBE_NETFLOW_ENABLED: "true"`, :88 `PROBE_FLOW_DEDUP: "prefer-netflow"`. cmd/collector/main.go:48 `const version = "1.3.0"`; CHANGELOG.md:3 `## 1.3.0 - 2026-07-04` (the NetFlow release). .github/workflows/docker.yml:99-100 `type=semver,pattern={{version}}` + `type=semver,pattern={{major}}.{{minor}}` — proves :1.2 stops updating once version is 1.3.0. README.md:286 `| :1.2 | moving major.minor | gets every 1.2.x patch automatically (default in docker-compose.yml) |`, README.md:199 'The pinned :1.2 tag tracks the latest 1.2.x patch automatically.'

**Suggested fix:** Bump docker-compose.yml to `xphox/firewall-collector:1.3` (or :1.3.0) and update the README tag table/run examples (lines 128, 199, 285-286) in the same commit; consider a CI guard test that the compose tag's major.minor matches the version const.

### LC-46 [MEDIUM] (server) — DATA-RETENTION.md 'outbound connections / third-party processors' section contradicts code: PagerDuty/Opsgenie/Teams SaaS delivery and threat-feed downloads are missing

**Status:** OPEN

**Location:** `docs/DATA-RETENTION.md:94` · Dimension: dead-stale

The GDPR/CCPA disclosure doc asserts the server makes outbound calls ONLY for SMTP email, Slack/Discord webhooks, the IRC bot, and SNMP polling. Since Tranche 2 (v0.11.7) the notifier also POSTs alert payloads (device names, IPs, alert messages) to hardcoded third-party SaaS endpoints events.pagerduty.com and api.opsgenie.com plus Microsoft Teams webhooks — for a compliance doc these are exactly the 'third-party processors' the section claims to enumerate. Separately, the opt-in threat-feed poller (v0.10.514) makes outbound HTTPS GETs to configured public feed URLs. A compliance officer using this doc to build a processor inventory (its stated purpose) would produce a wrong one.

**Evidence:** docs/DATA-RETENTION.md:94-98 'The server makes outbound network calls **only** for: **Alert delivery** the operator configures: SMTP email, Slack/Discord webhooks, and the built-in IRC bot. **SNMP polling** of the monitored devices' — vs internal/notifier/incident_channels.go:18-19 `pagerDutyEventsURL = "https://events.pagerduty.com/v2/enqueue"` / `opsgenieAlertsURL = "https://api.opsgenie.com/v2/alerts"` and :72-73 `func (n *Notifier) sendPagerDuty(...) { return n.PostJSONSigned(pagerDutyEventsURL, buildPagerDutyPayload(alert, ...), "") }`; internal/threatfeed/threatfeed.go:98 `req, err := http.NewRequestWithContext(ctx, http.MethodGet, feed.URL, nil)`. PRIVACY.md:29-32 already discloses threat feeds, showing DATA-RETENTION.md was simply not updated.

**Suggested fix:** Add PagerDuty/Opsgenie/Teams (naming the SaaS endpoints and what alert fields they receive) and the opt-in THREAT_FEEDS_* fetches to the outbound section, mirroring PRIVACY.md.

### LC-51 [MEDIUM] (collector) — Collector NetFlow pipeline has no firewall-event gate: ASA NSEL flow-update records are emitted as full flows on top of teardown records, double/multi-counting every byte — contradicting the repo's own 'teardown-only' Tranche 3 design

**Status:** OPEN

**Location:** `internal/netflow/record.go:400` · Dimension: gap-sweep

The project's own design record (docs/flow-protocol-research-2026-07-03.md, server repo) flags the ASA NSEL 'update+teardown double-count trap' in its vendor matrix and lists Tranche 4 item 7 as 'NSEL update-accumulate reconciliation (upgrade from teardown-only)' — i.e., the shipped Tranche 3 behavior is supposed to be teardown-only byte counting. The code never implemented that gate: emitRecord reads dec.fwEvent only to STORE it (record.go:485) and for the zero-counter skip check (record.go:447); there is no branch anywhere in internal/netflow that drops or discounts counters based on event type (grep for fwEvent.v/fwEvent.ok returns exactly those two sites; grep -i teardown across the netflow+flowdedup packages matches only a fields.go comment and tests). So an ASA configured with flow-export event-type all (a standard config) sends periodic flow-update records carrying IE 231/232 byte counters PLUS a teardown carrying the flow totals, and each becomes an independent relay.FlowSample whose bytes the server sums into rollups, top-talkers, and the data_exfil/capacity detectors — inflating ASA byte accounting by at least 2x (far more for long flows updated every interval, since updates accumulate). Secondary effect of the same missing gate: flow-create (event 1) records are also emitted as zero-byte rows, so every ASA session is counted as >=2 'flows' in every COUNT(*)-based aggregate. The v9 conformance suite only tests teardown (want 2) and denied (want 3) — event 5 is never exercised, which is how the divergence from the documented design went unnoticed.

**Evidence:** record.go emits counters regardless of event type: `case dec.initOctets.ok: fwdBytes = dec.initOctets.v // ASA: bytes only...` (internal/netflow/record.go:400-402) then `Bytes: fwdBytes * uint64(rate),` (record.go:475) and `FirewallEvent: uint8(dec.fwEvent.v),` (record.go:485) — the only two uses of fwEvent besides `eventPresent := dec.fwEvent.ok || dec.natEvent.ok` (record.go:447). fields.go:67 documents `ieFirewallEvent = 233 // NSEL: 0 ignore/1 created/2 deleted/3 denied/4 alert/5 update`. Design intent, server repo docs/flow-protocol-research-2026-07-03.md: vendor matrix row 'Cisco ASA/FTD (NSEL) ... update+teardown double-count trap' (line ~170) and Tranche 4 backlog item 7 'NSEL update-accumulate reconciliation (upgrade from teardown-only)' (line ~149). Tests cover only teardown/denied: `t.Errorf("FirewallEvent = %d, want 2 (teardown)", ...)` (internal/netflow/v9_test.go:578) and TestParseV9_ASADeniedZeroCounters (v9_test.go:599); no test sends event 5.

**Suggested fix:** Implement the documented teardown-only contract: when fwEvent is present, emit byte/packet counters only for event 2 (deleted/teardown); emit create/update/denied as zero-counter event rows (or drop create/update entirely) until the Tranche 4 update-accumulate reconciliation lands. Add an event-5 conformance test.

### LC-07 [LOW] (collector) — samplerCache has no size cap and no sweep — the one flow cache exempt from the memory-bounding discipline its siblings document, and it grows the persisted cache file forever

**Status:** OPEN

**Location:** `internal/netflow/template.go:220` · Dimension: netflow-ipfix

Every other network-fed map in the flow pipeline is explicitly capped with the same stated rationale: templateCache (maxTemplatesPerExporter=256 / maxTemplatesTotal=8192, template.go:20-21, plus a 30-min TTL sweep at template.go:184-198), seqTracker (maxSeqStates=4096, seq.go:19 'same memory-DoS rationale as the template-cache caps'), flowdedup (maxExporters=4096), and the rate limiter. samplerCache alone has no cap and is never swept: setSamplerRate/setDomainRate (template.go:249-272) insert unconditionally, keyed by (exporter IP, sourceID). Because templates DO expire and free cap headroom every 30 minutes, a rotating set of spoofed exporter IPs/ODIDs (allowlist is nil=allow-any until the fleet list is pushed, netflow.go:134) can accumulate sampler entries indefinitely across template-cap generations — entries for long-gone exporters are also never removed in normal operation. All of them are serialized by SaveTo every 5 minutes (template.go:362-374), so netflow-templates.json grows monotonically for the life of the install.

**Evidence:** template.go:249-261 `func (s *samplerCache) setSamplerRate(…) { … m[samplerID] = rate }` and template.go:265-272 `setDomainRate` — no len() check, contrast template.go:159-163 `if c.perExporter[key.exporterKey] >= maxTemplatesPerExporter { return putRejectExporter } if len(c.templates) >= maxTemplatesTotal { return putRejectTotal }`; sweep only exists on templateCache (template.go:184 `func (c *templateCache) sweep`); seq.go:18-19 states the shared rationale ('same memory-DoS rationale as the template-cache caps') that samplerCache violates; SaveTo template.go:362-374 persists every perDomain/perSampler entry unconditionally.

**Suggested fix:** Cap samplerCache at the template-cache scale (e.g. 8192 domains) and evict sampler entries in the maintenance sweep when their exporterKey no longer has any live template (or after a TTL since last rateFor/set).

### LC-08 [LOW] (collector) — clampFlowTimes validates only flow END — a plausible end lets an absurd or inverted flow_start (epoch-1970, negative duration, wrap-miscorrection) be stored verbatim

**Status:** OPEN

**Location:** `internal/netflow/record.go:635` · Dimension: netflow-ipfix

clampFlowTimes checks the derived END against ±15 min of receive time and, only in that clamped branch, bounds the duration to maxPlausibleFlowDurationMs (24h). When the end IS plausible, the function returns `time.UnixMilli(startMs)` with zero validation: startMs may be 0 (an exporter sending flowStartMilliseconds=0 with a sane flowEndMilliseconds yields flow_start=1970-01-01 and an implied ~56-year duration), may exceed endMs (negative duration — nothing enforces start<=end anywhere in resolveFlowTimes), or may be one 2^32-ms wrap off after v5's heuristic wrap corrections (resolveV5Times, v5.go:176-191, feeds this same function and can subtract uptimeWrapMs from startMs only). The doc comment on clampFlowTimes claims durations are kept only 'when it is believable', but believability is enforced exclusively in the broken-end branch. flow_start is persisted raw to the new v29 column and is the input to the planned Tranche 4 duration-aware bucketing and flow stitching — garbage stored now is the exact 'single biggest semantic difference from sFlow' failure the research doc (§ 'Top 5 things that would have shipped broken', item 3) set out to avoid.

**Evidence:** record.go:635-646: `func clampFlowTimes(startMs, endMs int64, now time.Time) … if skew := end.Sub(now); skew > flowTimeSkewLimit || skew < -flowTimeSkewLimit { end = now; if durationMs >= 0 && durationMs <= maxPlausibleFlowDurationMs { return end.Add(-…), end } return end, end } return time.UnixMilli(startMs), end` — the final return path applies neither the duration bound nor any start-side skew check; resolveFlowTimes (record.go:598-606) fills a missing side from the other but never orders or bounds start vs end.

**Suggested fix:** In the plausible-end branch, apply the same duration test: if endMs-startMs is negative or exceeds maxPlausibleFlowDurationMs, set start=end (or end-duration-cap) before returning, mirroring the clamped branch.

### LC-14 [LOW] (server) — ProcessTrap ignores the policy-resolved severity — per-rule severity overrides work for syslog/system/interface alerts but silently do nothing for trap alert types

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:331` · Dimension: alerting

resolveAlertConfig lets an AlertRule override Severity for any alert type (policy.go:201-203), and every other alert constructor uses it: ProcessSyslog sets `Severity: resolved.Severity`, as do CheckSystemStatus/CheckInterfaceStatus/CheckVPNStatus/CheckDeviceOffline. ProcessTrap resolves the config (line 314, and uses its CooldownMinutes/PolicyID/InMaintenance/channels) but then hardcodes the raw trap severity into the alert, so an operator who creates a rule for e.g. LINK_DOWN with severity=critical (or downgrades a noisy trap type to info) sees no effect — the alert always carries the parser's severity. Additionally the hard filter at line 306 (`trap.Severity != "critical" && trap.Severity != "warning"`) runs before policy resolution, so a rule can never opt an info-level trap type into alerting. The admin UI offers severity on rules for all alert types with no hint that trap types ignore it — a copy-paste sibling that diverged when policies were threaded through the alert paths.

**Evidence:** internal/alerts/alerts.go:314 `resolved := am.resolveAlertConfig(trap.DeviceID, siteID, models.AlertType(trap.TrapType))` then alerts.go:327-336: `alert := models.Alert{ ... Severity: models.Severity(trap.Severity), ... PolicyID: resolved.PolicyID, Suppressed: resolved.InMaintenance }` — resolved.Severity unused. Contrast ProcessSyslog alerts.go:432-441: `Severity: resolved.Severity`. Rule override honored in policy.go:201-203: `if rule.Severity != "" { resolved.Severity = rule.Severity }`. Pre-resolution filter alerts.go:306-308: `if trap.Severity != "critical" && trap.Severity != "warning" { return nil }`.

**Suggested fix:** Use resolved.Severity when a matching rule set one (fall back to trap.Severity otherwise), mirroring ProcessFlowDetection's documented fallback pattern at alerts.go:481-484; move the severity filter after policy resolution or treat a matching enabled rule as an opt-in.

### LC-18 [LOW] (server) — T2-8b role-aware UI gating is inert on all standalone admin pages: only admin.html loads admin-users.js, so html[data-role] is never stamped on probes/sites/irc/device-detail/connection-detail pages

**Status:** OPEN

**Location:** `web/admin/probes.html:181` · Dimension: rbac-authz

The role-gating mechanism (fetch /admin/api/me → stamp document.documentElement.dataset.role → CSS hides [data-min-role] controls) lives entirely in admin-users.js, which only admin.html includes. The standalone pages (probes.html, sites.html, irc.html, device-detail.html, connection-detail.html) load only admin-common.js, so no role is ever resolved there: every mutating control (add/edit/delete probe, regenerate key, IRC server management, connect/disconnect, site CRUD) renders for viewers and dead-ends in a 403. As a side effect, AdminCommon.sessionRole stays null on those pages, so the friendly insufficient_role error hardcodes the fallback and tells an OPERATOR 'Your role (viewer) doesn't allow this action' — a wrong role in the user-facing message.

**Evidence:** Script includes: web/admin/admin.html:238-239 loads both `admin-common.js` and `admin-users.js`; web/admin/probes.html:181, sites.html:507, irc.html:363, device-detail.html:594, connection-detail.html:247 load only `admin-common.js` (grep confirms admin-users.js appears in admin.html alone). admin-users.js:208-220: `AC.apiFetch(API_BASE + '/me').then(… document.documentElement.dataset.role = me.role …)` — the sole writer of the attribute the CSS gate keys on (admin-design-system.css:1093-1095 `html[data-role="viewer"] [data-min-role="operator"], … { display:none }` per its own comment 'admin-users.js stamps the session role on <html data-role>'). Wrong-role message: admin-common.js:380-381 `throw new Error('Your role (' + (window.AdminCommon && AdminCommon.sessionRole || 'viewer') + ") doesn't allow this action")` — sessionRole is only assigned in admin-users.js:210, never on standalone pages. Zero data-min-role tags exist in those five HTML files (grep empty), so no control is hidden for any role there.

**Suggested fix:** Include admin-users.js (or extract the /me-fetch + role-stamp into admin-common.js, which every page already loads) on the standalone pages, then tag their mutating controls with data-min-role; at minimum derive the role in the insufficient_role message from the server error instead of the 'viewer' fallback.

### LC-21 [LOW] (server) — Incident state machine dead-end: deleting a device with an open incident leaves the incident open forever — no code path or API can ever resolve it

**Status:** OPEN

**Location:** `internal/database/devices.go:165` · Dimension: db-migrations

An incident (v27/F12) is opened by the poller's AlertManager when DEVICE_OFFLINE fires and is resolved ONLY by closeIncident on that same device's recovery (incidents_f12.go:72-92). The incident HTTP surface is deliberately read-only ('Incidents (F12): read-only views ... Writes' — handlers_incidents.go:12; routes are two GETs at cmd/api/main.go:760-762). DeleteDevice (devices.go:165-171) removes only device_connections and the device row — it never resolves the device's open incident. The realistic operator flow 'device goes offline -> incident opens -> operator decommissions/deletes the device' therefore strands a permanently-open incident: GetOpenIncidents (`resolved_at IS NULL`, incidents.go:47) returns it on every poll cycle into the openIncidents cache (incidents_f12.go:35-46), it renders as an active incident in the UI indefinitely, and since the device row is gone no recovery can ever fire ResolveIncident(deviceID). Unreachable terminal state with zero-byte operator remedy.

**Evidence:** devices.go:165-171: `func (d *Database) DeleteDevice(id uint) error { ... tx.Where("source_device_id = ? OR dest_device_id = ?", id, id).Delete(&models.DeviceConnection{}) ... return tx.Delete(&models.Device{}, id).Error }` — no Incident handling. incidents_f12.go:83: `inc, err := am.db.ResolveIncident(device.ID)` is the ONLY ResolveIncident caller, reached solely from the device-recovery path. handlers_incidents.go:12-14: 'read-only views over the grouped alert storms'; cmd/api/main.go:761-762: only `admin.GET("/api/incidents", ...)` and `admin.GET("/api/incidents/:id/alerts", ...)` — no resolve/close write endpoint. incidents.go:45-49: GetOpenIncidents selects `resolved_at IS NULL` with no device-existence join.

**Suggested fix:** In DeleteDevice's transaction, resolve any open incident for the device (`UPDATE incidents SET resolved_at=now() WHERE device_id=? AND resolved_at IS NULL`), and/or add an admin resolve endpoint; optionally have refreshOpenIncidents/GetOpenIncidents drop incidents whose device_id no longer exists.

### LC-23 [LOW] (server) — syslog_messages is partitioned but its retention is DELETE-only — partition drop is skipped even when provably safe, on the table that dominates prod DB size

**Status:** OPEN

**Location:** `internal/database/cleanup.go:321` · Dimension: retention-cleanup,retention-cleanup

syslog_messages is one of the six monthly RANGE-partitioned tables (migrate.go:199-206), and AUDIT-028's design is 'drop whole old partitions first (instant, reclaims space)' with batched DELETE only for the straddling tail (cleanup.go:268-271). But the syslog cleanup deliberately never calls dropPartitionsOlderThan for syslog_messages, justified by 'a partition can hold rows under two different cutoffs'. That justification is wrong for partitions entirely OLDER than the longer of the two windows: when SyslogCriticalDays > 0 (the documented prod recommendation, RETENTION_SYSLOG_CRITICAL_DAYS=30), any partition whose upper bound is <= now - max(criticalDays, infoDays) contains only rows past BOTH cutoffs and is safe to drop. Instead, every expired row on the highest-volume table is removed via the 10k-row batched DELETE loop, which bloats the partitions (dead tuples + autovacuum churn — the exact failure mode AUDIT-028 partitioning was built to avoid), and fully-emptied monthly child partitions accumulate as catalog objects forever since nothing ever drops them.

**Evidence:** cleanup.go:321-324: '// AUDIT-028: ... syslog_messages is intentionally NOT partition-dropped here — its dual critical(<6)/info(>=6) retention means a partition can hold rows under two different cutoffs, so it stays on the severity-scoped DELETEs above.' vs migrate.go:199-206 `var partitionTables = []partitionDef{ {"interface_stats",...}, ..., {"syslog_messages", "timestamp"}, ... }` and cleanup.go:119-126 dropPartitionsOlderThan doc: 'It only ever drops a partition whose upper bound ... is <= cutoff'. The severity-scoped deletes at cleanup.go:297-313 are the only mechanism; a cutoff of now-max(SyslogCriticalDays, infoDays) with SyslogCriticalDays>0 satisfies both windows for wholly-aged partitions, so the intentional-skip rationale does not hold for them. docs/DATA-RETENTION.md:24-26 tells operators 'cleanup drops whole old partitions where it can' — untrue for syslog_messages.

**Suggested fix:** When ret.SyslogCriticalDays > 0, call dropPartitionsOlderThan("syslog_messages", now - max(SyslogCriticalDays, effectiveInfoDays)) before the severity-scoped DELETEs; keep the DELETE path for straddling partitions and for the SyslogCriticalDays==0 (keep-forever) case.

### LC-24 [LOW] (server) — DATA-RETENTION.md retention table omits three auto-pruned tables (flow_rollups holds src/dst IPs for 365 days) and still labels flow_samples 'sFlow samples' after NetFlow/IPFIX ingestion landed

**Status:** OPEN

**Location:** `docs/DATA-RETENTION.md:30` · Dimension: retention-cleanup,dead-stale

The doc opens with 'Every fact below is grounded in the code ... applied by CleanupOldData' but its per-table list (lines 30-49) is missing flow_rollups (RETENTION_FLOW_ROLLUP_DAYS, default 365 — contains SrcAddr/DstAddr conversation pairs, i.e. the same PII class the doc flags on flow_samples), flow_detections (RETENTION_FLOW_DETECTION_DAYS, 90), flow_agent_drops (RETENTION_AGENT_DROPS_DAYS, 30), and flow_if_counters — all pruned by CleanupOldData since the 2026-07-01 audit H4 fix. The incidents table added by migration v27 is likewise absent from the 'NOT auto-pruned' section. Additionally, row 39 describes flow_samples as 'sFlow samples' with sFlow PII notes, but since v0.11.20 (migration v29 flow_source) the same table stores NetFlow v5/v9/IPFIX records including unsampled ASA NSEL denied-flow events — a data-subject-access or privacy review scoped by this doc misses PII rows kept a full year.

**Evidence:** docs/DATA-RETENTION.md:7 'Every fact below is grounded in the code'; :39 '| sFlow samples (5-tuple, counts) | flow_samples | RETENTION_FLOW_DAYS | 365 | **Yes** ...' — no rows for flow_rollups/flow_detections/flow_agent_drops anywhere in the file. Code: internal/database/cleanup.go:263 `{&models.FlowRollup{}, "flow_rollups", ret.Days(ret.FlowRollupDays)}` and :288-296 batchedDeleteOlderThanOn for FlowDetection (detected_at) and AgentDrops (window_start); internal/config/config.go:378-380 `FlowRollupDays: getIntEnv("RETENTION_FLOW_ROLLUP_DAYS", 365)` etc.; internal/models/models.go:1080-1081 FlowRollup fields `SrcAddr string` / `DstAddr string`; internal/database/migrations.go:80 `{version: 29, name: "flow_ingest_columns", ...}` (flow_source on flow_samples).

**Suggested fix:** Add flow_rollups/flow_detections/flow_agent_drops/flow_if_counters rows (with PII flags on flow_rollups), rename the flow_samples row to 'Flow records (sFlow/NetFlow/IPFIX)', and list `incidents` under retention or the NOT-auto-pruned section.

### LC-28 [LOW] (server) — INTERFACE_ERRORS message prints unclamped unsigned counter deltas: a single counter reset while the total still rises yields ~1.8e19 'new errors' in the alert text

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/alerts/alerts.go:388` · Dimension: time-units-math

CheckInterfaceErrors correctly clamps the AGGREGATE delta against counter resets (alerts.go:371-373: `if totalErrors >= prevTotalErrors { errorDelta = totalErrors - prevTotalErrors }`), but the per-counter breakdown embedded in the alert message subtracts each uint64 pair raw: `iface.InErrors-prev.InErrors` etc. If one counter resets (device reboot mid-window, per-queue clear, 32-bit wrap on the device) while another grows enough that the summed total still increases, the gate passes (errorDelta > 0) and the message renders an underflowed value like 18446744073709551520 for the reset counter — garbage in the email/Slack notification and in Alert.Message forever. The same underflow can also inflate which counters LOOK responsible. The clamp discipline applied to the total two lines above (and everywhere else in the codebase per the 'clamp resets' interface-counter lesson) was not applied to the four operands of the message.

**Evidence:** alerts.go:368-373: `var errorDelta uint64\n\t\ttotalErrors := iface.InErrors + iface.OutErrors + iface.InDiscards + iface.OutDiscards\n\t\tprevTotalErrors := prev.InErrors + ...\n\t\tif totalErrors >= prevTotalErrors {\n\t\t\terrorDelta = totalErrors - prevTotalErrors\n\t\t}` (clamped) vs alerts.go:388: `Message: fmt.Sprintf("Interface %s has %d new errors/discards (in_err: %d, out_err: %d, in_disc: %d, out_disc: %d)", iface.Name, errorDelta, iface.InErrors-prev.InErrors, iface.OutErrors-prev.OutErrors, iface.InDiscards-prev.InDiscards, iface.OutDiscards-prev.OutDiscards)` (raw uint64 subtraction, no clamp).

**Suggested fix:** Compute each per-counter delta with the same >= clamp used for the total (helper: func clampDelta(cur, prev uint64) uint64) before formatting the message.

### LC-29 [LOW] (server) — TOTP replay guard (lastTOTPSlot) is per-process but its correctness comment claims an API singleton — an ALLOW_MULTI_API follower serves logins with its own empty replay map, and the OPERATIONS.md follower caveat list omits it

**Status:** OPEN

**Location:** `internal/auth/auth.go:88` · Dimension: concurrency-state

The TOTP single-use-per-slot replay guard added with 2FA (v0.10.568) is an in-process map: MarkTOTPSlotUsed records the last authenticated 30s slot per user in `lastTOTPSlot` and rejects a second use (auth.go:179-188). Its comment justifies in-memory storage with 'only ONE cmd/api process serves logins (AUDIT-040 singleton)' — but that premise is false under the documented ALLOW_MULTI_API follower mode, which serves the FULL HTTP surface including /api/auth login+TOTP (cmd/api/main.go:222 only gates IRC bots off; nothing gates auth routes). Each instance keeps an independent lastTOTPSlot map, so an intercepted still-fresh TOTP code can be replayed once per API instance, and the shared password+TOTP lockout budget (loginAttempts, same map class) doubles per instance. AUDIT-040 explicitly accepted lockout/rate-limit/uptime divergence and documented it (OPERATIONS.md:336-337 lists exactly 'Login-lockout, rate-limit, and uptime remain per-instance'), but the TOTP replay slot shipped later and was never added to that accepted-divergence list — the new single-use security property silently degrades in follower mode with a code comment asserting the opposite.

**Evidence:** internal/auth/auth.go:86-90: '// lastTOTPSlot records, per user, the most recent 30s TOTP time-slot that\n\t// successfully authenticated — a valid code is single-use within its slot\n\t// (replay guard). In-memory is sufficient: only ONE cmd/api process serves\n\t// logins (AUDIT-040 singleton).\n\tlastTOTPSlot map[uint]int64'. auth.go:179-188 MarkTOTPSlotUsed: `if am.lastTOTPSlot[userID] == slot { return false }`. cmd/api/main.go:222-223: 'WARNING: AUDIT-040 — starting in FOLLOWER mode (ALLOW_MULTI_API=true).' — follower proceeds to serve all HTTP; only main.go:441-448 gates IRC ('AUDIT-040: IRC bots disabled (follower...)'), no auth-route gating. docs/OPERATIONS.md:335-337 follower caveats: 'The extra instance serves HTTP but does not start the IRC bots. Login-lockout, rate-limit, and uptime remain per-instance and will diverge.' — TOTP replay guard absent from the list.

**Suggested fix:** Either persist the last-used slot on the admins row (one indexed UPDATE per successful 2FA login — unlike rate-limiting this is low-frequency), or minimally fix the comment and add the TOTP replay guard to the OPERATIONS.md follower-mode divergence list.

### LC-30 [LOW] (server) — SeasonalSpikeDetector.Observe holds the detector-wide mutex across a 30-day interface-history DB query — all concurrent pollDevice goroutines' spike checks serialize behind DB I/O on every profile refresh

**Status:** RESOLVED (server v0.11.23)

**Location:** `internal/report/spike.go:243` · Dimension: concurrency-state

Observe takes the single detector mutex `d.mu` (spike.go:234) and, while holding it, calls the injected `profileFor(key)` callback when a key's profile is nil or older than 6h (spike.go:243-248). In cmd/poller that callback is `p.db.GetInterfaceChartData(deviceID, ifIndex, "30d")` (cmd/poller/main.go:125) — a 30-day chart aggregation. pollDevice runs in up to 5 concurrent goroutines (main.go:511-527) and calls Observe once per up-interface per cycle (main.go:774), so at poller startup EVERY interface's first Observe triggers the heavy query while holding the global mutex, serializing all five poll workers behind one another's DB reads, and every 6h each interface's refresh stalls all concurrent spike evaluation again. The F17 baseline cache in the SAME poller was explicitly built to avoid exactly this pattern — baseline_f17.go:9-12: 'The cache has its own mutex and is REFRESHED OUTSIDE am.mu (ensureBaseline runs before the alert lock is taken), so the poller's alert path never holds the alert mutex across a DB query' — making this a sibling that diverged from the codebase's own stated locking rule. Blocking-only (no data corruption), but it delays SNMP poll completion and can push cycle time past the poll interval on installs with many interfaces or a slow/contended Postgres.

**Evidence:** internal/report/spike.go:233-248: `func (d *SeasonalSpikeDetector) Observe(...) SpikeDecision {\n\td.mu.Lock()\n\tdefer d.mu.Unlock()\n...\n\tif d.profileFor != nil && (st.profile == nil || now.Sub(st.profileAt) >= d.refreshInterval) {\n\t\tif p := d.profileFor(key); p != nil {` — DB callback invoked under d.mu. cmd/poller/main.go:119-129: `p.spikeDetector = report.NewSeasonalSpikeDetector(6*time.Hour, 30*time.Minute, func(key string) *report.SeasonalProfile {\n...\n\t\tbuckets, err := p.db.GetInterfaceChartData(deviceID, ifIndex, "30d")`. Concurrent callers: main.go:511-527 (`sem := make(chan struct{}, 5)` + `go func(device ...){ p.pollDevice(device) }`), Observe call at main.go:774 inside pollDevice. Contrast internal/alerts/baseline_f17.go:9-12 ('REFRESHED OUTSIDE am.mu ... never holds the lock across a DB query').

**Suggested fix:** Mirror the F17 pattern: check/refresh the profile outside the mutex (fetch the profile first, then lock to swap it in), or use a per-key lock so one interface's 30-day query doesn't block every other interface's Observe.

### LC-33 [LOW] (server) — Disable-2FA flow: server revokes every session (token-version bump) but the profile page shows success and keeps operating as if logged in — sibling enable flow handles the revocation, disable doesn't

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-profile.js:412` · Dimension: frontend-backend

Disable2FA increments the admin's token version after clearing TOTP (handlers_totp.go:304), and ValidateToken rejects any token whose version mismatches (internal/auth/auth.go:358-361) — so the caller's own session is dead the instant the 200 lands. The enable flow knows this: Verify2FA also bumps the version, and the wizard responds with holdSession(), a no-API recovery-codes step, and a forced 'Continue to sign in' exit (admin-profile.js:14-19, 239-244). The disable flow diverged: on success it shows a toast ('Two-factor authentication disabled'), flips me.totp_enabled, and re-renders the profile in place (admin-profile.js:413-419) with no navigation to login. The very next API call — including the background dashboard pollers that fire within seconds — gets a 401 and apiFetch yanks the tab to /admin/login (admin-common.js:351-367), so the user is dumped mid-page with no explanation, and the freshly rendered 'enable 2FA' button is a dead control. The UI renders a continuing-session state the backend can never produce after this call.

**Evidence:** Server: internal/api/handlers/handlers_totp.go:304 `if err := db.IncrementAdminTokenVersion(admin.ID); err != nil {` (in Disable2FA, right before the 200); internal/auth/auth.go:359-361 `currentVersion, err := am.db.GetAdminTokenVersion(claims.UserID); if err != nil || claims.TokenVersion != currentVersion {` → reject. JS: admin-profile.js:412-419 `AC.apiFetch(API_BASE + '/2fa/disable', ...).then(function () { ... AC.showSuccess('Two-factor authentication disabled'); renderIdentity(); render2FA(); })` — no redirect, no holdSession. Contrast enable path: admin-profile.js:241-243 stepDone: `'<p>For security, your sessions were signed out. Sign in again...' + '<button ... data-action="mfa-w-login">Continue to sign in</button>'`.

**Suggested fix:** After a successful /2fa/disable, mirror the enable flow: tell the user their sessions were signed out and navigate to /admin/login (or have the server re-issue a fresh session cookie in Disable2FA if staying logged in is the intent).

### LC-35 [LOW] (server) — Incident endpoints (T2/F12) have zero frontend consumers: the INC#id chip advertises grouping but /admin/api/incidents and /incidents/:id/alerts are never called, so incident state is invisible in the SPA

**Status:** OPEN

**Location:** `cmd/api/main.go:761` · Dimension: frontend-backend

Tranche 2's incident grouping registered GET /admin/api/incidents (ListIncidents) and GET /admin/api/incidents/:id/alerts (GetIncidentAlerts) at cmd/api/main.go:761-762, and alerts carry incident_id (models.go:277). The SPA renders a non-interactive chip on grouped alerts — 'INC#N — Grouped under incident #N — individual notification muted' (admin-main.js:1932-1934, 1983-1985) — but `grep -rn incidents cmd/api/static/js web/admin/admin.html` finds no fetch to either endpoint anywhere: no incident list, no way to see which alerts an incident absorbed, whether it's still open, or its one-line summary. Operators are told notifications were muted and given no page to inspect why or what else is inside the incident — the backend produces open/resolved incident states the UI cannot display, and the two handler routes are effectively dead code from the shipped product's perspective.

**Evidence:** Routes: cmd/api/main.go:761-762 `admin.GET("/api/incidents", handler.ListIncidents)` / `admin.GET("/api/incidents/:id/alerts", handler.GetIncidentAlerts)`. Model: internal/models/models.go:277 `IncidentID *uint \`json:"incident_id\"\`` and Incident struct at models.go:378-390 with StartedAt/ResolvedAt/AlertCount. UI: admin-main.js:1932-1934 `var incidentChip = a.incident_id ? ' <span class="badge critical" ... title="Grouped under incident #' + a.incident_id + ' — individual notification muted">INC#' + a.incident_id + '</span>' : '';` — a static span, no data-action, no link. `grep -rn "incidents" cmd/api/static/js/*.js web/admin/admin.html` → no matches (verified: command produced no output).

**Suggested fix:** Make the INC# chip clickable — a small modal fed by /admin/api/incidents/:id/alerts (title, started/resolved, absorbed alerts) closes the loop; or add an Incidents section to the alerts page using ListIncidents.

### LC-40 [LOW] (server) — config.env.example is declared 'the authoritative reference ... lists every variable' but is missing 47 env vars including every Tranche-2 knob (ALERT_FLAP_*, PAGERDUTY_ROUTING_KEY, OPSGENIE_API_KEY, TEAMS_WEBHOOK_URL, WEBHOOK_SECRET) and all SNMP_V3_* credentials

**Status:** OPEN

**Location:** `config.env.example:1` · Dimension: config-settings

README.md:368-370 states: 'The authoritative reference is config.env.example — it lists every variable with inline comments and defaults.' A mechanical diff of getEnv/getIntEnv/getBoolEnv/getFloatEnv/getDurationEnv keys in internal/config/config.go against VAR= lines in config.env.example shows 47 variables absent from the file, including the complete Tranche-2 alerting surface added in v0.11.1-.10 (ALERT_FLAP_MAX_FIRES, ALERT_FLAP_WINDOW_MINUTES, ALERT_FLAP_MIN_ACTIVE_SECONDS, PAGERDUTY_ROUTING_KEY, OPSGENIE_API_KEY, TEAMS_WEBHOOK_URL, WEBHOOK_SECRET, PROBE_DATA_LAG_ALERT_MINUTES), the entire SNMPv3 credential set (SNMP_V3_USERNAME/_AUTH_TYPE/_AUTH_PASS/_PRIV_TYPE/_PRIV_PASS — despite README advertising SNMPv3 support), spike detection (SPIKE_*), report scheduling (REPORT_*), DB tuning (DB_MAX_OPEN_CONNS, DB_SSL_MODE), and RETENTION_SYSLOG_DAYS. Grep confirms zero mention (even in comments) of SNMP_V3, webhook_secret, pagerduty, flap, or spike anywhere in the file. Additionally 16 of the 47 are the server-side PROBE_* ProbeConfig block (config.go:458-478) that is parsed but consumed by NO code — grep for `.Probe.` outside internal/config returns nothing since cmd/probe was removed — yet README.md:98-100 and docs/FEATURES.md:31 still advertise '[Server] Syslog receiver — TCP + UDP ... SYSLOG_ALLOWED_SOURCES allow-list' as a Stable feature although no server binary imports internal/syslog or internal/sflow (the unused receiver itself was noted as I9 in the 2026-06-23 audit, but the Stable-feature docs claim and the dead env vars were not).

**Evidence:** README.md:368-370: 'The authoritative reference is [config.env.example](config.env.example) — it lists every variable with inline comments and defaults.' comm -23 of sorted env keys from internal/config/config.go vs config.env.example yields 47 names, e.g. ALERT_FLAP_MAX_FIRES (config.go:446 `FlapMaxFires: getIntEnv("ALERT_FLAP_MAX_FIRES", 5)`), PAGERDUTY_ROUTING_KEY (config.go:449), SNMP_V3_USERNAME (config.go:330), SPIKE_STDDEV_THRESHOLD (config.go:443); `grep -c "SNMP_V3" config.env.example` = 0. Dead block: config.go:458-478 loads PROBE_SERVER_ENABLED..SFLOW_ALLOWED_SOURCES into ServerConfig-sibling ProbeConfig; `grep -rn '\.Probe\.' cmd/ internal/` (excluding internal/config) returns no consumers, and `grep -rn 'internal/sflow"\|internal/syslog"' --include='*.go'` finds no imports. README.md:98-100: '- **[Server] Syslog receiver** — TCP + UDP, RFC 5424 + RFC 3164, `SYSLOG_ALLOWED_SOURCES` allow-list.'

**Suggested fix:** Regenerate config.env.example from the config.Load() key list (or add a test that diffs them, mirroring the AUDIT-026 guard pattern); either delete the dead ProbeConfig block + internal/syslog + internal/sflow or re-tag the README/FEATURES rows as collector-side.

### LC-47 [LOW] (server) — Committed tasks/SFLOW-NOC-REDESIGN-PLAN.md (1,776 lines, 'Status: design / pre-implementation') contradicts the shipped roadmap: claims 0.11.0-0.15.0 are the sFlow-rewrite releases, explicitly excludes NetFlow/IPFIX, and describes long-fixed bugs as current

**Status:** OPEN

**Location:** `tasks/SFLOW-NOC-REDESIGN-PLAN.md:3` · Dimension: dead-stale

This superseded plan is still committed with an active-sounding status header and no superseded/archived banner (unlike tasks/archive/*). It asserts (a) 'Target ship window: 5 minor releases (0.11.0 → 0.15.0)' — but v0.11.0 shipped RBAC and v0.11.x is executing a 13-tranche program per docs/FEATURE-ROADMAP.md; (b) 'What we explicitly do NOT do: add ... IPFIX/NetFlow support' — Tranche 3 shipped exactly that (server v0.11.20 + collector v1.3.0); (c) the executive summary presents the bytes×sampling_rate under-counting bug as a current defect ('understates real traffic by 1:512 ... bytes never multiplied by sampling rate') though ingest now scales by sampling rate. A contributor or the next audit round reading tasks/ would plan against a dead design and believe a serious data-correctness bug still exists.

**Evidence:** tasks/SFLOW-NOC-REDESIGN-PLAN.md:3 '**Status:** design / pre-implementation', :7 '**Target ship window:** 5 minor releases (`0.11.0` → `0.15.0`)', :13 'understates real traffic by 1:512** (bytes never multiplied by sampling rate)', :17 'What we explicitly do NOT do: add Kafka, ClickHouse, ... IPFIX/NetFlow support'. Contradicted by docs/FEATURE-ROADMAP.md:328 'NetFlow v5/v9 + IPFIX ... ✅ SHIPPED server v0.11.20 + collector v1.3.0' and internal/database/flows.go:246 '// at ingest (collector + server parser both multiply by sampling_rate;'. File is tracked: `git ls-files tasks/` lists it outside tasks/archive/.

**Suggested fix:** Move to tasks/archive/ with a SUPERSEDED banner pointing at docs/FEATURE-ROADMAP.md Part IV and the shipped sFlow analytics R1 work, or delete it.

### LC-48 [LOW] (collector) — Collector README tells users the sibling server ships a `fwmon-probe` binary (removed) and that 'the current release' is 1.2.x

**Status:** OPEN

**Location:** `README.md:28` · Dimension: dead-stale

The repo-comparison table in the collector README lists the server's binaries as `fwmon-api, fwmon-poller, fwmon-trap, fwmon-probe`, but cmd/probe was removed from the server (server cmd/ contains only api, poller, trap-receiver, configcheck; server FEATURES.md explicitly documents the removal). An operator provisioning the HQ side from this table will look for a binary/Docker target that does not exist. Line 35 also anchors the entire feature list to 'the current `1.2.x` release' although the current release is 1.3.0 and the list itself includes 1.3.0-only NetFlow features (line 67), making the version framing self-contradictory.

**Evidence:** Firewall-Collector README.md:28 '| Binaries | `firewall-collector`, ... | `fwmon-api`, `fwmon-poller`, `fwmon-trap`, `fwmon-probe` |' and :35 'Every feature below is shipped in the current `1.2.x` release.' vs server /Users/xphox/Projects/Firewall-Mon/docs/FEATURES.md:198 '| Binaries built | 3 fwmon daemons | `cmd/{api,poller,trap-receiver}` (`cmd/configcheck` is a CLI; `cmd/probe` was removed) |' and server cmd/ listing (api, configcheck, poller, trap-receiver only). Collector cmd/collector/main.go:48 `const version = "1.3.0"`; README.md:67 documents the '[Both] NetFlow v5/v9 + IPFIX receiver' (1.3.0 feature).

**Suggested fix:** Drop fwmon-probe from the server binaries cell and change '1.2.x' to '1.3.x' (or an unversioned phrase) in the same doc pass as the compose tag bump.

### LC-49 [LOW] (server) — admin-main.js legacy Flows fallback is advertised as a working safety net but crashes on removed DOM ids and renders 8-column rows into the 9-column table added in v0.11.20

**Status:** OPEN

**Location:** `cmd/api/static/js/admin-main.js:115` · Dimension: dead-stale

The flows page router keeps the pre-v0.10.211 loadFlows()/loadFlowCharts() path 'as a fallback in case admin-flows.js fails to load', and three data-action handlers ('set-flow-range', 'load-flows', 'load-more-flows') still branch to it. The fallback no longer works: loadFlowCharts dereferences ids removed from admin.html ('flows-sources', 'flows-dests', 'flows-sampling-rate', plus 'flow-range-label'/'flows-filter-protocol' used elsewhere) — the unguarded `.textContent` on a null element throws, the .catch swallows it, and no charts/stat cards render. renderFlowsTable also emits 8 cells per row while the current #flows-table header has 9 columns (the 'Source' flow_source column added with NetFlow in v0.11.20), so even the table it does render is misaligned. ~200 lines of dead-but-reachable code whose comment misleads the next developer into believing there is graceful degradation.

**Evidence:** admin-main.js:115-124 '// The legacy loadFlows()/loadFlowCharts() path is left below as a fallback in case admin-flows.js fails to load. ... else { loadFlows(); }'; admin-main.js:1463-1466 `document.getElementById('flows-sources').textContent = ...` / `'flows-dests'` / `'flows-sampling-rate'` — none of these ids exist in web/admin/admin.html (stat cards at admin.html:564-569 are only flows-total/flows-bytes/flows-throughput/flows-packets/flows-protocols; the sampling chip is id 'flows-sampling-rate-chip', admin.html:557). admin-main.js:1551-1567 renderFlowsTable builds 8 <td> per row and `colspan="8"` empty state vs admin.html:742 header `<th>Time</th><th>Src IP:Port</th><th></th><th>Dst IP:Port</th><th>Protocol</th><th>Source</th><th class="num">Bytes</th><th class="num">Packets</th><th class="num">Rate</th>` (9 columns).

**Suggested fix:** Delete the legacy loadFlows/loadFlowCharts/renderFlowsTable/setFlowRange/loadMoreFlows block and the fallback branches (show a plain 'failed to load flows module' error instead), or actually maintain the fallback against the current DOM including the flow_source column.

### LC-52 [LOW] (server) — Denied-flow visibility — 'the headline NetFlow feature' — is write-only: firewall_event has zero read consumers (no API filter, no UI column, no detector, no alert) even inside the ~1h raw retention window

**Status:** OPEN

**Location:** `internal/database/flows.go:128` · Dimension: gap-sweep

The v0.11.20 changelog ships 'firewall_event (IE 233 — denied-flow visibility)' as the marquee Tranche 3 win and the ingest comment calls it 'the headline NetFlow feature', but the column has no read path anywhere in the product: FlowStatsFilter — which the changelog says 'mirror[s] the filters the Flow Samples list honors' and drives every Flows-page view — gained FlowSource in v29 but has no FirewallEvent member; no handler accepts a firewall_event query param; grep across all Go/JS/HTML finds firewall_event only in the pgx COPY column list, the v29 migration DDL, and the ingest comment; admin-flows.js and admin.html contain no denied/firewall-event rendering; and internal/detect/internal/alerts never reference it (finding 1). This is distinct from the already-reported rollup finding (flows.go:677, forensic columns destroyed after ~1h): even during the raw window, an operator cannot filter, list, chart, export, or be alerted on a denied flow — the feature the release notes verified end-to-end ('denied-flow events' confirmed landing in Postgres) is observable only via manual SQL. A changelog/product contract vs code inconsistency, not a feature request: the shipped Source filter shows the team wired read paths for the other new column but skipped this one.

**Evidence:** FlowStatsFilter ends at FlowSource with no firewall_event dimension: `FlowSource *uint8 // models.FlowSource* (0=sFlow,1=v5,2=v9,3=IPFIX); nil = all` (internal/database/flows.go:128-141). `grep -rn firewall_event --include='*.go' --include='*.js' --include='*.html'` (excluding tests/models) matches only internal/database/flow_samples_pgx.go:51 (`"firewall_event",` COPY column), internal/database/migrate.go:1337 (v29 ADD COLUMN), and the handlers_data.go:254 ingest comment; `grep -rn -i 'denied|firewall' cmd/api/static/js/admin-flows.js` matches nothing. CHANGELOG.md v0.11.20: '`firewall_event` (IE 233 — **denied-flow visibility**...)' and 'Flows page gains a **Source filter** (select + chip + URL param + CSV column + samples-table column)' — source got the full read path, firewall_event got none.

**Suggested fix:** Add a firewall_event filter to FlowStatsFilter + GET /admin/api/flows and a Denied badge/column + filter chip on the Flows page (mirroring the just-shipped flow_source plumbing); longer term the Tranche 4 deny-storm detector gives it an alert path.

