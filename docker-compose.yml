version: '3.8'

services:
  firewall-mon:
    image: firewall-mon:latest
    container_name: firewall-mon
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./data:/data
      - ./config:/config
