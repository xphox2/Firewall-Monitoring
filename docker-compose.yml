version: '3.8'

services:
  fortigate-mon:
    image: fortigate-mon:latest
    container_name: fortigate-mon
    restart: unless-stopped
    ports:
      - "8080:8080"
      - "162:162/udp"
    volumes:
      - fortigate_data:/data

volumes:
  fortigate_data:
