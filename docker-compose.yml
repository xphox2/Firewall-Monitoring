version: '3.8'

services:
  fortigate-mon:
    image: fortigate-mon:latest
    container_name: fortigate-mon
    restart: unless-stopped
    expose:
      - "8080"
    volumes:
      - fortigate_data:/data

volumes:
  fortigate_data:
