version: '3.8'

services:
  fortigate-mon:
    image: fortigate-mon:latest
    container_name: fortigate-mon
    restart: unless-stopped
    network_mode: "host"
    volumes:
      - ./data:/data

volumes:
  data:
